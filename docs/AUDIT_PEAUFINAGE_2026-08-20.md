# Audit peaufinage pré-partage — 2026-08-20

> Audit 4 axes en données réelles (multi-agents : 189 tests d'URLs images, SQL prod,
> 18 mesures TTFB, lecture de code notifs/crons), déclenché par le constat Rudy :
> « MVs manquants, images cassées, pages lentes, notifs doublées, ergonomie ».
> Synthèse et plan de correction : JOURNAL 2026-08-20 + BACKLOG.
> Format brut par axe : [sévérité] fait / evidence, puis fixes proposés [effort S/M/L].

########## NOTIFS ##########
[bloquant_partage] Doublon structurel prouvé : un event mv/release qui sort DEMAIN part deux fois à 15 min d'écart. Le digest (10:30 UTC) le liste car sa fenêtre daily est [now, now+48h) sans filtre de type ; l'alerte comeback (10:45 UTC) le renvoie en 'day_before' car son jour local == demain. Exemple concret : drop 2026-08-21 18:00 KST → 20/08 12:30 Paris « Tomorrow: aespa — X » (digest) puis 12:45 « ⏳ Tomorrow: aespa — X » (alerte).
evidence: send-digest/route.ts:21 (DAILY_WINDOW_MS=48h), :51-53 + :72-73 (gte now / lt now+48h, aucun filtre type) ; notify-comebacks/route.ts:63-67 (mv/release, fenêtre [now-1j, now+3j)) ; comebacks.ts:90-96 (resolveKind: startKey == today+1 → day_before) ; .
[bloquant_partage] Aucune dédup cross-cron n'existe : les deux idempotences sont disjointes et aveugles l'une à l'autre. digest_log dédup par (user, jour, édition) — il empêche seulement de renvoyer LE digest ; event_notifications dédup par (user, event, kind) — il empêche seulement de renvoyer LA même alerte. Aucune des deux routes ne lit la table de l'autre, et buildDigest ne reçoit jamais l'ensemble des events déjà/bientôt alertés.
evidence: send-digest/route.ts:76 + :195-200 (digest_log, clé user_id/day_key/edition) ; notify-comebacks/route.ts:101-112 + :147-151 (event_notifications, clé user:event:kind) ; buildDigest (digest.ts:144-185) n'a aucun paramètre alreadySent/alerted
[important] Doublon J-0 aussi possible le même jour : le digest inclut tout event dont start_at > 10:30 UTC du jour (gte now), donc un drop à 20:00 KST (11:00 UTC) est dans le digest ET reçoit « 🔥 Out now » 15 min après. Total pour un comeback tardif : jusqu'à 4 push (digest J-1, ⏳ J-1, digest J, 🔥 J), dont 2 paires à 15 min d'écart.
evidence: send-digest/route.ts:72 (.gte('start_at', now)) ; comebacks.ts:93 (startKey == todayKey → day_of) ; crons.yml:21-22
[important] L'édition weekly (lundi, fenêtre 7 j) aggrave le cas : chaque mv/release de la semaine est annoncé 3 fois minimum (hebdo lundi + ⏳ J-1 + 🔥 J-0), le lundi même les events J0/J1 doublent à 15 min.
evidence: send-digest/route.ts:22 + :44-53 (WEEKLY_WINDOW_MS=7j, lundi UTC) ; comebacks.ts:69 (2 kinds par event)
[important] Empilement dans le tiroir : les tags sont bien posés MAIS ne fusionnent jamais digest et alerte du même event. tag='digest' (le digest du jour remplace celui d'hier — OK) et tag='comeback-<eventId>' (J-1 remplacé par J-0 — OK) coexistent ; renotify:true fait re-buzzer chaque remplacement. Résultat perçu : 2 notifs empilées pour le même comeback + buzz à chaque fois.
evidence: digest.ts:128 et :140 (tag:'digest') ; comebacks.ts:107 (tag:`comeback-${event.id}`) ; public/sw.js:23 (...(tag ? { tag, renotify: true } : {}))
[important] Esthétique — le payload ne transporte QUE title/body/url/tag ; le SW hardcode une icône générique identique pour toutes les notifs. Pas d'`image` (grande visuelle), pas d'`actions`, pas d'icône par artiste alors que groups.image_url / image_landscape / banner_url existent en DB (déjà SELECT dans send-digest pour les anniversaires). Toutes les notifs KStage se ressemblent : icône app + texte.
evidence: send.ts:8 (PushPayload = title/body/url/tag) ; digest.ts:38, comebacks.ts:71 (mêmes types) ; public/sw.js:14-26 (icon:'/icons/icon-192.png' et badge:'/icons/badge-96.png' hardcodés, aucun image/actions) ; send-digest/route.ts:110 (colonnes images dis
[mineur] Le socle icon/badge est en revanche CONFORME aux bonnes pratiques : icon-192.png (192px — web.dev : « an icon size of 192px or more is a safe bet ») et badge-96.png monochrome dédié (web.dev : « 24px multiplied by the device pixel ratio… 72px or more » ; MDN badge : « about 96 by 96 px, and the image will be automatically masked »). Fichiers présents et légers (5 Ko / 1 Ko).
evidence: public/icons/: icon-192.png (5049 o), badge-96.png (1032 o), icon-512.png ; sources testées : https://web.dev/articles/push-notifications-display-a-notification et https://developer.mozilla.org/en-US/docs/Web/API/Notification/badge
[mineur] Mineur : timestamp: Date.now() est posé à l'affichage — pour des notifs datées (« Out now »), l'horodatage de l'event (start_at) serait plus juste dans le tiroir Android qui trie par timestamp.
evidence: public/sw.js:24
--- fixes ---
[M] Dédup cross-flux PAR USER dans buildDigest (le cœur du fix) : exclure du digest les events mv/release dont le jour local de l'abonné est aujourd'hui ou demain — exactement le prédicat resolveKind ≠ null. Exporter resolveKind (ou un helper isComebackAlerted(event, now, timeZone)) depuis comebacks.ts et l'appliquer dans le filtre userEvents de digest.ts:176-179 (le filtre doit être par user car il d
src: code interne (comebacks.ts:90-96 resolveKind, digest.ts:176-179 filtre userEvents)
[S] Étendre PushPayload + sw.js pour transporter l'esthétique : champs optionnels icon, image, actions dans PushPayload (send.ts:8) et les types Digest/ComebackPayload ; sw.js lit payload.icon/image/actions avec fallback sur les valeurs actuelles (icon: payload.icon ?? '/icons/icon-192.png', ...(payload.image ? { image: payload.image } : {}), ...(payload.actions ? { actions: payload.actions } : {})).
src: MDN Notification API (options icon/image/actions de showNotification) — https://developer.mozilla.org/en-US/docs/Web/API/ServiceWorkerRegistration/sho
[M] Payload comeback enrichi par artiste : dans notify-comebacks/route.ts, ajouter image_url, image_landscape, banner_url au SELECT groups (ligne 46) et poser icon = URL absolue de groups.image_url (photo carrée self-hostée, déjà ≥192px après le pipeline resize) et image = image_landscape ?? banner_url (web.dev : Android « a width of 450dp… an image of width 1350px or more would be a good bet », deskt
src: web.dev push notifications — https://web.dev/articles/push-notifications-display-a-notification (« icon size of 192px or more is a safe bet », image 4
[S] Actions de notification : actions: [{ action: 'open', title: 'View' }] sur les comebacks (cible = event.url) et [{ action: 'calendar', title: 'Open calendar' }] sur le digest, + handler event.action dans notificationclick (sw.js:30-45) qui route vers data.url. Rester à 1-2 actions texte (limite navigateur getMaxActionCount, icônes d'action facultatives et monochromes si ajoutées un jour).
src: web.dev push notifications (action buttons, « ensure action icons work as monochrome graphics ») + MDN NotificationAction
[S] Calmer le buzz : retirer renotify pour le tag 'digest' (1 notif/jour, le remplacement silencieux suffit) et le garder uniquement sur comeback-<id> (le J-1 → « Out now » mérite un buzz). Concrètement : le SW applique renotify seulement si payload.renotify === true, et seuls les payloads comeback le posent.
src: MDN Notification.renotify (renotify exige tag ; contrôle du re-buzz au remplacement) — https://developer.mozilla.org/en-US/docs/Web/API/Notification/r
[S] Vérification post-fix en conditions réelles : déclencher via workflow_dispatch send-digest puis notify-comebacks (crons.yml accepte l'input endpoint) sur un jour avec un mv/release J-1 connu, et confirmer sur téléphone qu'UNE seule notif porte l'event (l'alerte), que le digest ne le cite plus, et que scrape_log/digest_log/event_notifications restent cohérents. Ne pas se contenter des tests vitest
src: code interne (.github/workflows/crons.yml:25-40 workflow_dispatch, tables digest_log/event_notifications) + règle projet « vérifier avant de claim »
########## PERF ##########
[mineur] Le TTFB serveur est BON partout : 0,130-0,236 s sur les 9 URLs, froid comme chaud. Le backend (colocalisation cdg1/eu-west-3, requetes bulk paralleles) n'est pas la cause de la lenteur ressentie.
evidence: curl -w time_starttransfer, 2 passes x 9 URLs : / 0,236/0,130 s ; /groups 0,160/0,140 s ; /calendar 0,149/0,154 s ; /mvs 0,141/0,132 s ; /search 0,135/0,135 s ; /groups/aespa 0,168/0,194 s ; /artists/taemin 0,180/0,156 s ; /mv/jennie-mantra-official-
[important] Aucune page n'est JAMAIS servie du cache CDN : 18 requetes sur 18 (y compris les 2e passes) sont des MISS avec un Cache-Control qui interdit tout cache, meme pour les visiteurs anonymes et les 2 201 URLs du sitemap crawlees par les bots. Chaque vue = invocation serverless + requetes Supabase completes.
evidence: Header 'x-vercel-cache: MISS' + 'Cache-Control: private, no-cache, no-store, max-age=0, must-revalidate' sur les 18 requetes ; cause : getViewer()/getViewerTimeZone() lisent cookies() en haut de chaque page (ex. src/app/groups/(list)/page.tsx:35-42)
[bloquant_partage] PIRE PAGE /groups : 549 629 o de HTML decompresse (34 181 o sur le fil), 0,535 s de telechargement complet a froid, 215 balises <img>, ~5 083 tags (Lighthouse alerte des ~1 400 noeuds) et 126 808 o de payload RSC inline. Cause : les DEUX onglets (groups + solo, ~172 items) sont charges et rendus a chaque requete, et chaque item est serialise 3 fois (items + followedItems + trendingEntries) dans les props du composant client GroupsTabs — donc present dans le HTML ET dans le flight RSC.
evidence: curl + analyse de groups.html ; code : src/app/groups/(list)/page.tsx:32-53 (commentaire 'Les DEUX jeux sont charges d'un coup') et :89-100 (toTabData duplique sorted.map en followedItems/items + trendingEntries, x2 onglets)
[important] 2e PIRE /calendar : 370 822 o de HTML dont 180 213 o (49%) de payload RSC inline, 0,457 s en passe CHAUDE (plus lent qu'a froid — zero cache). Cause : le mois ENTIER d'events est serialise dans les props du composant client CalendarFilterProvider, et la liste complete des groupes est rendue DEUX fois (GroupFilter dans SidebarLeft + MobileGroupFilter).
evidence: curl + analyse de calendar.html (rsc_bytes=180 213, tags=3 814) ; code : src/app/calendar/page.tsx:61-66 (events passes en props client) et :80-90 (GroupFilter monte 2 fois)
[important] 3e PIRE /groups/aespa : 218 273 o de HTML pour une fiche groupe (0,296 s total). Meme classe de probleme : payload RSC massif (events + MVs + membres + ratings serialises vers les composants client) sur une page pourtant identique pour tous les visiteurs anonymes.
evidence: curl : 218 273 o, TTFB 0,168/0,194 s ; code : src/app/groups/[slug]/page.tsx:120-124 (getGroupPageData -> events, mvs, members, followCounts tous serialises)
[important] L'image LCP n'est JAMAIS prioritaire : 100% des images de la home (11/11) et de /groups (215/215) sont loading="lazy", zero attribut priority/fetchpriority sur tout le site. L'image du hero (au-dessus du fold) attend le JS + le calcul de viewport avant de charger — penalite LCP directe selon web.dev.
evidence: grep sur home.html et groups.html : loading="lazy" = 11 et 215 occurrences, fetchpriority = 0 occurrence, srcset = 0 occurrence ; les images passent bien par next/image + loader Cloudinary f_auto,q_auto (data-nimg present) mais sans variante responsi
[mineur] Poids JS : 17 chunks, 1 154 647 o decompresses / 360 824 o compresses charges sur chaque page. Correct pour une app Next mais s'additionne a l'hydratation des gros payloads RSC ci-dessus sur mobile.
evidence: curl de chaque /_next/static/immutable/chunks/*.js reference par home.html, somme size_download brut et --compressed
[mineur] Waterfalls serveur mineurs (10-50 ms vu la colocalisation, PAS la cause principale) : /groups enchaine 2 Promise.all sequentiels ; la home connectee enchaine jusqu'a 5 allers-retours (getViewer -> [timeZone, followedIds] -> Promise.all(6) -> getGroupMvs hero -> getRatingsForEvents) ; /mvs en fait 3.
evidence: src/app/groups/(list)/page.tsx:35-53 ; src/app/(home)/page.tsx:45,80,82-93,101,119 ; src/app/mvs/page.tsx:40,45-52,56-60
[mineur] Le sitemap ne contient AUCUNE URL /show/ ni /calendar (2 201 URLs : 1 000 /mv/, 1 000 /artists/, 196 /groups/, 5 racines) — les pages episodes verifiees 200 (ex. /show/m-countdown/2026-08-13) sont invisibles pour l'indexation. Constat hors-perf releve en passant.
evidence: grep sur https://kstage.app/sitemap.xml telecharge : 0 occurrence de /show/, 2 201 <loc>
--- fixes ---
[S] Ajouter priority (et retirer le lazy implicite) sur l'image LCP de chaque page : HeroBackdrop de /mvs, NextDropCard/hero de la home, banniere des pages /groups/[slug] et /mv/[slug], et les ~8 premieres tuiles visibles de /groups. Une ligne par composant, gain LCP immediat.
src: web.dev/articles/lcp-lazy-loading (le lazy-load de l'image LCP degrade le LCP) + docs Next.js next/image prop 'priority' (a mettre sur l'image LCP de
[M] /groups : rendre cote serveur UNIQUEMENT l'onglet actif et un premier lot (~48 tuiles) ; le 2e onglet et la suite de la grille se chargent au clic/scroll (route API deja envisageable ou simple pagination client sur des donnees allegees). Reduire les props de GroupsTabs a une seule liste par onglet (id, name, slug, image, nextEvent) et deriver followedItems/trending cote client au lieu de serialise
src: web.dev 'DOM size and interactivity' (Lighthouse signale > ~1 400 noeuds) + docs Next.js 'Server and Client Components' (chaque prop passee a un compo
[M] /calendar : degraisser les 180 Ko de RSC — ne serialiser vers CalendarFilterProvider que les champs utilises par le filtrage/l'affichage (id, type, start_at, title, group slug/name/color) au lieu des rows completes, et ne monter GroupFilter qu'UNE fois (le doublon desktop/mobile se resout en CSS ou en partageant la meme instance via children).
src: code interne (src/app/calendar/page.tsx:80-90, deux montages de GroupFilter avec la meme liste) + docs Next.js sur la serialisation des props client
[M] Etendre le patron unstable_cache deja en place (getGroupsCached, revalidate 3600, src/lib/groups/queries.ts:16-30) aux requetes identiques pour tous les viewers : getNonSoloGroups, getSoloArtists, getNextEventForGroups, getRecentReleasesForGroups, getGroupFollowCounts, getCalendarMonthEvents, getAllMvs (revalidate 300-900 s + tags), avec revalidateTag appele en fin de cron scraping. Les pages rest
src: docs Next.js unstable_cache + revalidateTag (cache de donnees partage entre requetes, independant du rendu dynamique) ; patron deja valide dans le cod
[S] Ne PAS prioriser l'optimisation des waterfalls serveur (les 2 Promise.all de /groups, les 5 etapes de la home) ni le TTFB : mesures a l'appui, le serveur repond en 130-240 ms et chaque aller-retour DB coute ~5-15 ms en colocalisation. Traiter d'abord payload RSC + LCP lazy + cache de donnees ci-dessus, puis re-mesurer en conditions reelles (PageSpeed Insights / CrUX sur mobile) avant d'aller plus
src: web.dev Core Web Vitals (LCP/INP se gagnent cote client quand le TTFB est deja < 0,8 s 'good') + mesures curl de cette session
[S] Ajouter les pages /show/[show]/[day] (et /calendar) au sitemap genere par src/app/sitemap.ts — hors perf mais releve pendant l'extraction des slugs : ces pages 200 n'y figurent pas.
src: code interne (grep du sitemap.xml prod : 0 URL /show/)
########## MVS ##########
[important] Le check thin_mv_catalogs (109 groupes non dissous, ≤5 MVs visibles) se décompose : 12 à exactement 5 MVs, 97 à <5, 19 à 0 MV. Répartition sources : 99/109 ont déjà ≥1 source YouTube seedée (119 sources type youtube_api, 100 % avec channel_id, toutes scrapées le 2026-08-20 et deep-scannées en août) ; seuls 10/109 sont à 0 source : MISAMO, NCT DOJAEJUNG, DK X Seungkwan, ASCENDER, HEARTSTEEL, Aria, V8, NCT JNJM, SUCTION, Cherish. 9 groupes ont une source ET 0 MV.
evidence: SQL prod (execute_sql) : total_thin_le5=109, exactly_5=12, cron_pool_lt5=97, zero_source=10, has_source=99, has_source_but_zero_mv=9, zero_mv=19 ; jointure sources : 119 sources youtube_api, no_channel_id=0
[bloquant_partage] Cause racine n°1 — le cron discover-channels ne fait RIEN pour un groupe déjà sourcé : il ne considère que candidates[0] et si cette chaîne est déjà en base il skippe (« source déjà présente ») sans backfiller ni essayer candidates[1]. Or 99/109 groupes du check sont déjà sourcés → sur les 5 derniers runs (50 visites), 29 visites = « source déjà présente » (no-op garanti à ~201 unités quota pièce), ~10 = « 1 seul MV matché », 6 seeds seulement, 15 MVs gagnés au total.
evidence: src/app/api/cron/discover-channels/route.ts:90-102 (const best = discovery.candidates[0]) + src/lib/scrapers/channel-discovery.ts:188-195 (if existing → return seeded:false sans backfill) ; scrape_log source='channel_discovery' 2026-07-20→2026-08-17
[important] Cause racine n°2 — off-by-one : le cron cible mvCount < 5 (THIN_CATALOG_MVS=5 avec strict <) alors que le check santé flagge ≤ 5. Les 12 groupes à exactement 5 MVs (ALLDAY PROJECT, Hwasa, XLOV, RESCENE, ODD YOUTH, BADVILLAIN, SAY MY NAME, Chung Ha, DAILY:DIRECTION, ASC2NT, STARSEED'Z, N-ID) sont flaggés en permanence mais JAMAIS visités par la découverte : leur temps de drain est infini par construction.
evidence: src/app/api/cron/discover-channels/route.ts:26 (THIN_CATALOG_MVS=5) et :68 (mvCount.get(g.id) ?? 0) < THIN_CATALOG_MVS, vs src/lib/health/checks.ts:39+283 (n <= THIN_MV_THRESHOLD) ; SQL : exactly_5=12
[bloquant_partage] Cause racine n°3 — mauvaise chaîne seedée : 13 groupes du check ne sont accrochés qu'à une chaîne umbrella partagée par ≥3 groupes (JYP 32,2M, 1theK 25,1M, HYBE LABELS 82,4M, Stone Music 11,2M, Starship 6,7M, BPM…). Le deep-scan plafonne à 20 pages = 1000 uploads récents : sur ces chaînes à dizaines de milliers d'uploads, les MVs plus anciens sont mécaniquement inatteignables (Baek A Yeon : 1 MV en base vs 10+ réels sur JYP ; ADYA : 0 MV vs ≥5 réels sur Stone Music ; ZICO : 4 vs 10+ sur HYBE LABELS).
evidence: SQL : thin_on_umbrella_channel=13 ; src/app/api/cron/scrape-youtube/route.ts:18-19 (DEEP_PER_RUN=15, DEEP_MAX_PAGES=20) ; src/lib/scrapers/youtube.ts:280-284 documente les chaînes impaginables type 1theK >20k uploads
[bloquant_partage] Cas KATSEYE vérifié en conditions réelles : sa propre chaîne (13,1M subs, UCA61H4fWOMHikLcUFKyQUog) est seedée, scrapée quotidiennement et deep-scannée le 2026-08-10, et le groupe a 0 MV en base — à raison : ses 500 derniers uploads ne contiennent AUCUN titre de MV (que des Shorts/contenu ; seuls hits MV-ish = « MV Commentary » et « MV (Choreography Ver.) », des dérivés correctement rejetés). Ses vrais clips (Gnarly, Gabriela, Touch…) vivent sur HYBE LABELS, non seedée pour KATSEYE — et la visite discovery skipperait probablement car sa propre chaîne sort en tête des candidats déjà présents.
evidence: API YouTube playlistItems (playlist UUA61H4fWOMHikLcUFKyQUog, 10 pages, 500 titres scannés le 2026-08-20) : 0 titre MV officiel ; SQL : katseye mv_count=0, 1 source, deep=2026-08-10, scraped=2026-08-20
[bloquant_partage] Les 15 pires cas « groupe connu × catalogue honteux » (subs = max par groupe, souvent la chaîne du label) : 1) KATSEYE 0 MV (13,1M, debut 2024-06-28) ; 2) MISAMO 0 MV, 0 source, confidence=candidate (sous-unité TWICE, debut 2023-07-26) ; 3) ZICO 4 MVs (82,4M, réel 10+) ; 4) Baek A Yeon 1 MV (32,2M via JYP, réel 10+) ; 5) ADYA 0 MV (11,2M via Stone Music, debut 2023) ; 6) Taemin 3 MVs (543k — seule la chaîne BPM est seedée, toute l'ère SMTOWN absente : Move, Criminal, Advice…) ; 7) Chung Ha 5 MVs (366k, réel 12+) ; 8) PLAVE 4 MVs (1,2M, réel 10+) ; 9) TRI.BE 1 MV (650k, debut 2021, réel ~8) ; 10) EVNNE 3 MVs (652k, réel ~7) ; 11) Hwasa 5 MVs (1,99M, réel ~8 solo) ; 12) ALLDAY PROJECT 5 MVs (4,31M THE BLACK LABEL) ; 13) GIRLSET 4 MVs (32,2M JYP + chaîne propre 1,04M) ; 14) Dragon Pony 4 MVs (25,1M via 1theK impaginable) ; 15) NCT DOJAEJUNG 0 MV, 0 source (debut 2023). Cas limite : Jisoo 3 MVs (101M = chaîne BLACKPINK) mais son catalogue solo réel est ~3-4 → plutôt faux positif du seuil que trou de data.
evidence: SQL prod : jointure thin×sources avec subscriber_count, last_deep_scan_at, last_scraped_at par groupe (requête du 2026-08-20)
[bloquant_partage] Temps de drain réaliste au rythme actuel : il ne converge PAS. Pool cron = 97 groupes, 10 visites/lundi → cycle complet ≈ 10 semaines ; conversion observée 12 % (6 seeds/50 visites) avec +2,5 MVs/seed → un seed laisse souvent le groupe sous le seuil (il reste dans le pool) ; sorties nettes du check ≈ 0-1/semaine, pendant que 37/109 groupes ont débuté en 2026 (inflow continu) → le compteur monte (109, « en hausse »). Même borne théorique à 100 % de conversion : ≥ 10-11 semaines. Les 12 groupes à 5 MVs et la majorité des 99 déjà-sourcés ne peuvent structurellement jamais sortir via ce cron.
evidence: Chiffres mesurés : scrape_log 5 runs (2026-07-20→08-17) ; SQL cron_pool_incl_disbanded=97, thin_debut_2026=37, thin_debut_recent_90j=16 ; route.ts:23 MAX_GROUPS_PER_RUN=10, cron hebdo lundi
[important] Marge quota disponible : la découverte du lundi consomme ~2 020 unités observées, le scrape quotidien ~1 500-1 900 (321 sources youtube_api à ~4-5 unités + deep 15×~21) sur un plafond de 10 000/jour → il reste ~×2-3 de marge le lundi. MAIS élargir MAX_GROUPS_PER_RUN sans corriger le no-op « source déjà présente » gaspillerait ~58 % du budget supplémentaire (proportion observée de visites no-op).
evidence: scrape_log units=2010-2038/run ; SQL total_yt_api_sources=321 ; coût par appel : search.list=100 unités, playlistItems/channels/videos.list=1 unité (quota YouTube Data API v3)
[mineur] Métrique à nuancer : 16/109 groupes ont débuté il y a moins de 90 jours (dont OURBIRTHDAY 22/07, AEN 05/08) — leur catalogue fin est légitime et pollue le signal du check ; et le classement « popularité » par max(subscriber_count) est trompeur car il reflète souvent la chaîne du LABEL (Jisoo 101M = chaîne BLACKPINK, dodree/Baek A Yeon 32,2M = JYP), pas la notoriété du groupe.
evidence: SQL : thin_debut_recent_90j=16 ; lignes sources jointes (jisoo→@BLACKPINK 101M, baek-a-yeon→@jypentertainment 32,2M)
--- fixes ---
[S] Corriger le no-op « source déjà présente » dans /api/cron/discover-channels : itérer sur discovery.candidates et seeder le PREMIER candidat ≥2 hits dont le channel n'est pas déjà en base (au lieu de candidates[0] seul) ; si tous les candidats sont déjà seedés, déclencher scrapeGroup(source existante, {maxPages: 20}) à la place du skip sec (~20-40 unités, déjà payées 200 en search). Transforme ~58
src: code interne (src/app/api/cron/discover-channels/route.ts:90-102, src/lib/scrapers/channel-discovery.ts:188-195)
[S] Aligner le seuil du cron sur le check : `(mvCount.get(g.id) ?? 0) < THIN_CATALOG_MVS` → `<=` (route.ts:68), avec le commentaire mis à jour. Débloque les 12 groupes coincés à exactement 5 MVs dont le drain est aujourd'hui impossible.
src: code interne (route.ts:26+68 vs src/lib/health/checks.ts:39+283)
[M] Passe one-shot ciblée sur les ~20 pires cas nommés (KATSEYE, MISAMO, ZICO, Baek A Yeon, ADYA, Taemin, Chung Ha, PLAVE, TRI.BE, EVNNE…) : seed manuel du bon canal via la méthode éprouvée youtube-channels.json + scripts/discover-mv-channels.ts, et scripts/backfill-mv-search.ts (recherche par titre, bypass pagination) pour les groupes accrochés aux chaînes impaginables (1theK, JYP, HYBE LABELS, SMTOW
src: code interne (scripts/backfill-mv-search.ts référencé dans src/lib/scrapers/youtube.ts:280-284 ; docs/SCRAPING.md §3.19) + quota YouTube Data API v3 (
[S] APRÈS le fix du no-op seulement : élargir le débit — MAX_GROUPS_PER_RUN 10→20 (≈4 100 unités/lundi, tient sous 10 000 avec le quotidien à ~1 900) ou passer le cron GitHub Actions à 2 lundis-équivalents/semaine. Cycle complet du pool : 10 semaines → ~5, avec des visites désormais toutes productives. L'élargir AVANT le fix ne ferait que doubler le gaspillage.
src: code interne (route.ts:23, crons.yml GitHub Actions — pas de limite Vercel Hobby ici) + quotas YouTube Data API v3
[S] Assainir la métrique pour qu'elle mesure le vrai retard : exclure du check thin_mv_catalogs les groupes à debut < 90 jours (16 aujourd'hui), en les listant dans un check info séparé. Le compteur cessera de monter mécaniquement à chaque vague de debuts 2026 et redeviendra actionnable (93 vrais retards au lieu de 109).
src: code interne (src/lib/health/checks.ts:274-292 ; patron data-health = anti-récurrence, cf. reference_data_health_monitor)
########## IMAGES ##########
[important] Aucune URL morte dans les 4 colonnes du brief : 91/91 URLs échantillonnées répondent 200 (groups.image_url 30, banner_url 1, image_landscape 30, members.photo_url 30), et vérification EXHAUSTIVE côté storage : 1297/1297 références supabase (1211 members + 85 groups + 1 banner) pointent vers un objet existant dans storage.objects — 0 orphelin. L'hypothèse « URLs mortes en DB » est infirmée.
evidence: curl GET -o /dev/null max-time 10 sur 91 URLs = 91×200 ; SQL LEFT JOIN storage.objects = missing_in_storage: 0 partout
[mineur] i.scdn.co (Spotify) n'expire PAS : les 160 URLs groups.image_url sont des blobs content-addressed immuables ; 20/20 testées = 200, y compris via l'optimizer Vercel prod et avec Referer kstage.app. Idem theaudiodb (30/30), yt3.googleusercontent (30/30), i.ytimg default.jpg (40/40 dont les 10 plus anciens), kpopofficial (28/28). Aucun hotlink-blocking constaté. fandom/static.wikia = 0 URL en DB (héritage déjà purgé par le self-host à l'ingestion).
evidence: 189 tests curl (91 + 98) = 189×200 ; avec header Referer https://kstage.app/ : 28/28 = 200 ; /_next/image prod (kstage.app) : 200 sur scdn/kpopofficial/supabase/ytimg
[bloquant_partage] LA classe cassée mesurée : les thumbnails maxresdefault.jpg construites côté serveur depuis le videoId — 18/90 = 404 (et 16/30 = 53 % sur les MVs les plus anciens ; hqdefault = 90/90 = 200). YouTube ne génère pas la maxres pour les vieilles vidéos. Surfaces : home hero (src/app/(home)/page.tsx:122), /mvs hero (src/app/mvs/page.tsx:90), next-drop-card. Le repli onError→hqdefault (src/components/home/hero-backdrop.tsx:28) est client-only : un 404 reçu avant l'hydratation (image priority = LCP, chargée immédiatement) ne re-déclenche pas onError → fond hero absent/vide. C'est le mécanisme le plus probable derrière les « images cassées » vues.
evidence: 90 videoIds testés (60 random + 30 plus anciens events type=mv) : maxres 72×200 + 18×404 stables au retest ; optimizer prod renvoie 404 text/plain (« NOT_FOUND cdg1:: ») pour ces maxres → l'img client reçoit une erreur
[mineur] Deux trous latents dans l'allowlist next/image (next.config.ts:69-79) : www.theaudiodb.com absent (2 rows image_landscape : AB6IX, A.C.E — seul r2.theaudiodb.com est déclaré) et yt3.googleusercontent.com absent (216 rows banner_yt_url). Inoffensif AUJOURD'HUI (ces colonnes sont rendues unoptimized via ArtistHero, et les 2 groupes www ont 22 MVs + banner_yt donc le chemin image_landscape de next-drop-card n'est pas atteint), mais tout futur rendu optimisé de ces hosts = throw Next « hostname is not configured » → 500 de rendu, pire qu'une image cassée.
evidence: next.config.ts:69-79 (grep remotePatterns) ; SQL : 2 rows www.theaudiodb.com, 216 rows yt3.googleusercontent.com ; artist-hero.tsx:32 et group-card/queue-row/mv-card = unoptimized ; hero-backdrop.tsx = optimisé mais n'atteint image_landscape que si b
[important] Aucun check data-health « URLs mortes » n'existe : la seule dérive détectable aujourd'hui l'est par l'œil de Rudy. Or le monitor /admin/health + checks.ts est le mécanisme anti-récurrence acté du projet.
evidence: src/lib/health/checks.ts (data-health monitor existant, cf. /admin/health) ne contient aucun check HTTP d'images ; mesure de cette session = 219 requêtes, ~40 s, trivialement automatisable
--- fixes ---
[M] Supprimer la dérivation maxresdefault au runtime : à l'ingestion/refresh (cron refresh-images, src/lib/images/refresh.ts), tester une fois maxresdefault et stocker la meilleure variante RÉELLEMENT servie (maxres sinon hqdefault) dans une colonne dédiée (ex. events.hero_image_url) ; les heros lisent cette colonne au lieu de construire l'URL. Élimine 100 % des 404 runtime et la dépendance au repli c
src: code interne (lib/images/refresh.ts + pattern feedback_idempotency_mutable_key : colonne dédiée pour tout enrichissement) ; comportement YouTube maxre
[S] Check data-health « URLs mortes » échantillonné dans src/lib/health/checks.ts : par (colonne, host) — groups.image_url, image_landscape, banner_yt_url, members.photo_url, events.image_url + la variante hero dérivée — tirer ORDER BY random() LIMIT 15, HEAD avec timeout 5 s, statut WARN si >2 % non-200, avec le détail X/Y par host affiché dans /admin/health. Borne le coût à ~90 requêtes par run (cro
src: code interne (reference_data_health_monitor : chaque bug data corrigé devient un check dans le même lot)
[S] Boucher l'allowlist next.config.ts : ajouter { protocol: 'https', hostname: 'yt3.googleusercontent.com' } et normaliser en DB les 2 URLs www.theaudiodb.com → r2.theaudiodb.com (même chemin /images/..., testé 200 sur r2), plutôt que d'ajouter un host pour 2 rows. Supprime le risque de throw « hostname is not configured » si un composant futur rend ces colonnes en optimisé.
src: Doc Next.js images.remotePatterns (nextjs.org/docs/app/api-reference/components/image#remotepatterns) : host non déclaré = erreur de rendu, pas un fal
[S] Rendre le repli hero robuste à la course d'hydratation : dans HeroBackdrop, après montage vérifier img.complete && img.naturalWidth === 0 (erreur survenue avant hydratation) et basculer sur fallbackSrc — ou passer le src pré-validé du fix n°1 qui rend ce garde-fou inutile. Ne pas se fier uniquement à onError sur une image priority.
src: MDN HTMLImageElement.complete/naturalWidth (developer.mozilla.org/en-US/docs/Web/API/HTMLImageElement/complete) : seul moyen fiable de détecter une er
