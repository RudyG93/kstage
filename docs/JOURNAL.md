# JOURNAL — journal de bord KStage

> **Une entrée par lot mergé sur `main`** (ou décision structurante), la plus récente en haut. L'état _courant_ du projet vit dans `PROJECT.md §9` ; ici c'est l'historique daté — le pourquoi, la vérification, les décisions. Règle d'entretien : `CLAUDE.md` § Journal de bord.
>
> Format : `## AAAA-MM-JJ — titre` puis **Branche/commit** · **Quoi** · **Pourquoi** · **Vérification** · **Décisions**.

## 2026-07-19 — Lot J React Compiler : programme « Normes modernes » clos

**Branche/commit** : `perf/react-compiler` (merge `6b64939`) → `main`.

**Quoi** : `reactCompiler: true` (stable v16) + `babel-plugin-react-compiler` (devDep) — mémoisation automatique des composants client, remplace les `useMemo`/`useCallback` manuels au fil de l'eau. Le pré-filtre SWC de Next ne passe le plugin Babel que sur les fichiers JSX/hooks → coût de build localisé.

**Vérification** : build 8,7 s (référence 8,6 s — impact nul), 758 vitest (mode CI), e2e prod 27 passed / 1 skipped (identique à la référence) ; le golden path auth (surfaces optimistes follow/like) est couvert par la CI GitHub post-merge. Arbitre INP : Speed Insights à J+7.

**Bilan du programme (10 lots, 2026-07-18 → 19)** : A (LCP/quick wins), B (typedRoutes), C-0 (clés API, trempage → C-1/C-2 ≥ 20/07), D (supply chain + gate admin), E (fonts), F (rails/shell Suspense), G (5 routes détail streamées anti-soft-404), H (proxy.ts), J (React Compiler) **livrés** ; I (cacheComponents) **no-go documenté** (entrée ci-dessous).

## 2026-07-19 — Lot I `cacheComponents` : NO-GO documenté (l'invariant soft-404 prime)

**Branche/commit** : `feat/cache-components` (`b209d59`, **parquée non mergée** — l'essai complet y est committé). `main` intact.

**Quoi (l'essai)** : flip `cacheComponents: true` + conversions `unstable_cache` → `'use cache'` + `cacheLife('hours')`/`cacheTag` (getGroupsCached, getAllMembers, searchGroupList, allGroupsForClient, allGroupRefs) + footer caché (`new Date()` interdit dans un shell prerendu) + exemptions `unstable_instant = false` sur 15 routes bloquantes (5 détail = invariant 404, privées/auth, browse tissées viewer). **Build vert 49/49**, routes détail en Partial Prerender, pages statiques (about/login/signup/terms/privacy) avec vrais shells.

**NO-GO (matrice curl, build prod local)** : slug bidon → **200** + UI not-found streamée + `noindex` sur les 5 routes détail — le soft-404 canonique proscrit par l'audit 2026-07-10 ; et le redirect canonique (`blackpink-lisa`) → 200 client-side au lieu de 307. Mécanisme (source Next lue : `dynamic-rendering.js`, `create-component-tree.js`, `instant-config.js`) : le shell fallback PPR (layout + chrome) est servi AVANT le check d'existence → le statut HTTP est committé ; `unstable_instant = false` n'exempte que la **validation de build**, pas le serving du shell ; `dynamic = 'force-dynamic'` postpone le segment sans supprimer le shell. Le critère d'ABORT du plan (« si le flip force un boundary au-dessus du check d'existence → revert, l'invariant prime ») s'applique → abandon propre.

**Conditions de reprise** : (a) décision produit d'accepter le 404 streamé (200 + noindex) sur les pages détail, OU (b) un mécanisme Next de statut-avant-shell par route (fallback shell désactivable), OU (c) espace de slugs fermé (`generateStaticParams` + `dynamicParams=false` — incompatible avec des groupes ajoutés en continu). La branche parquée rebuild verte telle quelle si retenté.

**Décisions** : programme « Normes modernes » : Lots A→H livrés, Lot I = no-go assumé (pas un échec du plan : le gate a joué son rôle), Lot J (React Compiler) reste optionnel et indépendant du flip.

## 2026-07-19 — Programme « Normes modernes » : Lot G clos (routes 4-5/5 : artists + profil)

**Branche/commit** : `perf/streaming-artists-profile` (merge `7692571`) → `main`.

**Quoi** : patron anti-soft-404 appliqué aux 2 dernières routes détail. ① `getMemberBySlug` wrappé `cache()` (partagé generateMetadata/page — parité avec `getGroupBySlug`/`getEventBySlug`). ② `/artists/[slug]` : les DEUX branches streamées — solo : hero + agency/liens + fiche immédiats depuis la row, follow (viewer) en slot `fallback=null`, corps (events, MVs, carrière) en `SoloBody` sous Suspense ; membre : header + fiche immédiats, corps (solo releases, carrière, groupmates) en `MemberBody` sous Suspense. Seuls awaits bloquants = existence + résolution du redirect canonique. ③ `/u/[username]` : header identité immédiat depuis la row profile ; avatar owner (uploadable) en slot à fallback = Avatar statique 64 px (zéro shift), actions owner (Admin, PushBell, réglages) en slot `fallback=null` re-vérifiant le viewer côté serveur, corps (stats, pickers, notes, liked MVs, followed groups) en `ProfileBody` sous Suspense.

**Vérification** : tsc + prettier + vitest 758 (mode CI) + build ; matrice curl en build prod : bogus → **404**, `taemin` (solo) → 200 events/MVs streamés, `babymonster-ahyeon` (membre) → 200 groupmates streamés dans le HTML final, `blackpink-lisa` → 307 `/artists/lisa`, `/u/bogus` → 404, `/u/RemilioTest` → 200 Liked MVs ; e2e CI=1 : 27 passed / 1 skipped ; review adversariale multi-agents (3 lentilles → réfutation 2 votes/finding) : **0 finding confirmé** (l'unique soulevé — skeleton pouvant s'effondrer sur un corps membre vide — réfuté : dernier élément du layout, aucun contenu décalé). CI GitHub du merge : verte (vérifiée via l'API).

**Restes du programme** : Lot I (flip `cacheComponents` — prérequis structurels tous en place), Lot J optionnel (React Compiler), C-1/C-2 clés après trempage (≥ 20/07).

## 2026-07-18 (nuit) — Programme « Normes modernes » : Lot F + Lot G (3 routes/5)

**Branche/commit** : `perf/streaming-rails-shell` (merge `3995706`) + `perf/streaming-detail-pages` (merge `b76925e`, un commit par route) → `main`.

**Quoi** : ① **Lot F** : `PageRails` auto-suspendu (gate viewer interne pour SidebarLeft, SidebarRight sous Suspense, `RailSkeleton` à espace réservé) — le fan-out des sidebars ne bloque plus le premier octet ; sidebars des 4 pages listes idem ; **layout racine SYNCHRONE** (cluster user extrait en `HeaderViewer` async sous Suspense, fallback dimensionné) — le shell ne dépend plus d'aucune donnée, forme requise par cacheComponents. ② **Lot G routes 1-3** — patron anti-soft-404 : le SEUL await bloquant = check d'existence (mémoïsé, partagé avec generateMetadata), `notFound()` AVANT tout streaming, corps sous `<Suspense>` : `/mv/[slug]` (shell player immédiat ; catalogue = `getRailData` cache() partagé rail/mobile), `/groups/[slug]` (hero immédiat à slots streamés — ComebackTag, HeroMeta upgradée, GroupFollow ; `getGroupPageData` cache() ; `ArtistHero.meta` élargi à ReactNode), `/show/[show]/[day]` (`getEpisodeLineup` cache() partagé header/corps).

**Vérification** : matrice curl par route (slug bidon → **404**, réel → 200, contenu streamé présent dans le HTML, redirect solo 307 intact) ; 27/28 e2e verts en build prod (golden path auth inclus) ; rituel CI-mode. CI GitHub des 2 merges : **vertes** (confirmées via l'API le 19/07).

**Restes du Lot G** : `/artists/[slug]` (2 branches) et `/u/[username]` ; ensuite Lot I (flip `cacheComponents` — prérequis structurels tous en place), Lot J optionnel, C-1/C-2 clés après trempage (≥ 20/07).

## 2026-07-18 (soir) — Programme « Normes modernes » : tranche 1/2 (Lots A, B, D, E, H sur 10)

**Branche/commit** : 5 merges → `main` : `perf/lcp-images-waves` (`f7adf45`), `chore/typed-routes` (`c14fae8`), `sec/supply-chain-admin-docs` (`6006636`), `chore/middleware-to-proxy` (`4290cdb`), `perf/fonts-cls` (`66307f8`). Plan complet (10 lots A→J, basé docs officielles `node_modules/next/dist/docs` + doc Supabase) dans le plan-file — la tranche 2 (F rails/shell Suspense, G streaming pages détail, I flip `cacheComponents`, C clés API après action Rudy, J React Compiler optionnel) suit.

**Quoi** : ① **Lot A quick wins mesurés** : le poster YouTube de la page MV (élément LCP !) chargeait en `loading="lazy"` → `fetchpriority="high"` ; `priority` sur la bannière ArtistHero ; `images.qualities [70,75]` (le `quality={70}` du hero était silencieusement forcé à 75 par le défaut v16) ; vagues série réduites (/mv 5→3, /artists membre 4→2) ; `getGroupFollowCounts()` mémoïsé (le RPC tournait 2×/render sur home et /groups, 6 call sites) ; comments-realtime ne refresh plus les onglets cachés. ② **Lot B** : `typedRoutes: true` (stable v16) — props partagées typées `Route`, casts aux constructions dynamiques ; un href interne cassé = erreur tsc. ③ **Lot D** : Dependabot (npm hebdo groupé + github-actions) + step CI `npm audit` report-only ; **gate admin central** `src/app/admin/layout.tsx` (`requireAdminPage()`, les 9 pages passent au helper, gardes conservées = défense en profondeur) ; `SECURITY_AUDIT.md` rafraîchi (4 items « ouverts » corrigés depuis juin). ④ **Lot H** : `middleware.ts` → `proxy.ts` (dépréciation v16 soldée, runtime Node = prérequis cacheComponents) — golden path auth vérifié en build prod. ⑤ **Lot E** : `display: 'optional'` sur les 3 familles décoratives (Space Grotesk/Instrument Serif/Archivo — familles intouchées, seule la stratégie de chargement change) → zéro layout-shift de swap ; arbitre CLS = Speed Insights J+3.

**Vérification** : rituel CI-mode par lot (`GITHUB_ACTIONS=true TZ=UTC` vitest + tsc + build) ; auth e2e prod-build sur H. CI GitHub des 5 merges (+ `8697b2b` Lot 1bis) : **vertes** (confirmées via l'API le 19/07).

**Aussi (après-midi)** : Lot 1bis livré (`8697b2b` — getClaims local, les signing keys étaient DÉJÀ ECC) ; décisions Rudy exécutées : solos = page SOLO canonique (Lisa/Jennie/Taemin/Hwasa/Solar, 307 vérifiés), suction-mj→astro-mj + NCT liés (`duplicate_person_candidates` = 0), épisodes fantômes MB 19/26 juin masqués (404 vérifié).

## 2026-07-18 — Round « Infrastructure & Data » : 11 lots (région, images, data-health, canonical, aliases, épisodes, roster watch, catalogue, calendrier client, fil Reddit, polish)

**Branche/commit** : 11 merges → `main` : `perf/region-auth-roundtrips` (`bc8fcf4`), `fix/member-image-pipeline` (`6aee9cd`), `feat/data-health` (`1bb97f4`), `fix/person-canonical-linking` (`80c24f2`), `feat/group-aliases-stage-matching` (`1b7b0fc`), `fix/episode-backfill` (`8075744`), `feat/predebut-roster-watch` (`7bab384`), `feat/mv-catalog-depth` (`9b2cc5a`), `perf/streaming-cwv` (`d244d14`), `ui/comment-thread-rail` (`65cfaca`), `ui/polish-rails-hover-back` (`69dc672`). Migrations **0061** (`groups.name_aliases`) et **0062** (`sources.last_deep_scan_at`) appliquées via MCP.

**Quoi** : ① **Perf infra** : fonctions Vercel épinglées `cdg1` (Paris — Supabase est à `eu-west-3`, chaque requête traversait l'Atlantique depuis `iad1`) + `getViewer()` mémoïsé (4 allers-retours auth/nav → 1) + Speed Insights. ② **Images** : normalisation sharp (≤800 px webp q80) sur les 3 chemins self-host — la photo SuA pesait **17,2 Mo** (Cloudinary fetch refuse >10 Mo → avatar cassé) ; 856 objets retraités (**~2 Go gagnés**) ; photos membres IMMÉDIATES à la création de groupe (la rotation ~100/j laissait idntt sans avatars un jour entier). ③ **Data-health monitor** : 12 checks des classes d'erreur connues (photos, objets lourds, catalogues maigres, numéros/stages d'épisodes, titres placeholder, rosters squelettiques, doublons de personnes…), page `/admin/health` + résumé quotidien scrape_log via le cron monitor — le mécanisme anti-« ça revient sans cesse ». ④ **Personnes canoniques** : `isSamePerson` (real_name OU birthday+stage, garde anti-homonymes née du dry-run réel : NewJeans Minji ↔ Dreamcatcher JiU = deux « Kim Minji ») + auto-lien à l'ingest + sweep : `uau-sua/jiu/yoohyeon` → rows Dreamcatcher (redirect 307 vérifié, page unique par personne). ⑤ **Aliases** (0061) : hangul officiel + « membre facturé » (« Ice Cream - 연준 » = slot TXT) dans le matching lineups ET stage-links → Music Bank du 17/07 : 12/15 → **15/15 stages**. ⑥ **Épisodes** : numérotation par AUTORITÉ Wikipedia (Chart winners) — le carrd était décalé (le « 1295 » du 17/07 = **épisode 1299**) : 9 comblés + 4 corrigés, validateur qui refuse une autorité incohérente (cas réel Music Core #952 dupliqué) ; le cron ne peut plus écraser un numéro existant ; backfill stages passés (fenêtre paramétrable + pagination profonde early-stop) : **+82 stages** (89 units). ⑦ **Roster watch** : re-scrape fandom append-only des (pre)debuts ≤30 j dans le cron quotidien — testé en réel : OURBIRTHDAY +2 membres (Baby, Achiraya) avec photos ; upgrade de titre placeholder dans comeback-ingest (« X debut » → vrai single, 6 cas en prod) ; debut ingest passe `tentative` (la fusion near-dup peut promouvoir) ; garde MusicBrainz pays (les 4 « membres » italiens d'ANTARES venaient du groupe eurodance homonyme). ⑧ **Catalogue MV** : sweep budgété (12 seedés, Catch The Young +13), cron discovery 3→10/lundi seuil ≤5, deep re-scan rotatif des sources (0062) — UNIS 4→6 MVs + fix cap silencieux 1000 rows du count, dédup sources par channel_id (7 paires purgées), garde anti-homonyme discovery (hits antérieurs au debut = pas de seed). ⑨ **Calendrier client** : changement de mois SANS navigation URL (route `/api/calendar/month` CDN 5 min, cache mémoire, prefetch adjacents, `replaceState`) + grille groups en `content-visibility:auto` sous la fold. ⑩ **Fil commentaires v4 Reddit** : rail sous l'avatar longeant TOUT le commentaire (enfants compris), clic = repli — vérifié au browser (géométrie + interactions). ⑪ **Polish** : /mv en 3 colonnes (catalogue du groupe en rail droit sticky), BackButton avec `fallbackHref` (nouvel onglet → destination naturelle), token `--hover` lisible sur les 2 thèmes (12 call-sites — `--muted` ≈ `--card` rendait les survols invisibles).

**Data one-shots** : OURBIRTHDAY = single « Hungry (Side A) » (Soompi 16/07) + « Yu » (mis-capture) retiré ; ANTARES purgé des 4 rows italiennes ; idntt **vérifié à 20 membres réels** (MODHAUS — la suspicion « sur-capté » du 17/07 était fausse) ; liens canoniques restants (Taemin/Jennie/Hwasa/Solar solos, NCT units, suction-mj) en ATTENTE de décision Rudy (direction canonique = choix produit).

**Vérification** : vitest 727 verts + build par lot ; prod : header `cdg1::` ✓, SuA 43 Ko + redirect ✓, MB 1295 15/15 (oembed) ✓, /admin/health = burn-down du round. **Incident CI** : rouge de `8075744` à `65cfaca` (e2e `link`→`button` du pager calendrier + prettier post-eslint-fix) — corrigé en fin de round, cf. commit suivant.

**Décisions** : releases à 18:00 KST = **correct et sourcé** (standard digital coréen, réforme Gaon 2017 ; les dates sans heure affichent déjà « Time TBA ») ; hébergeur : **on reste Vercel + Supabase** (le problème était la région + l'architecture, pas la plateforme) ; sub-units = canonical linking, pas de remodelage join-table.

## 2026-07-17 — Round de retours #2 : 7 lots (profil, admin, romanisation, couverture, page épisode)

**Branche/commit** : 6 merges → `main` : `fix/profile-admin-search-quick-wins` (`7b232af`), `fix/profile-picker-full-list` (`3f11a04`), `feat/romanized-titles` (`9bcb93a`), `feat/lineup-unmatched-pipeline` (`b235cc1`), `feat/musicbrainz-enrichment` (`fa47bc3`), `chore/lot-m-channels` (`05e0b93`) + **`feat/show-episode-page` (`43e08e8`, poussée, MERGE EN ATTENTE de la migration 0060** — MCP Supabase indisponible en fin de session). Décisions Rudy : one-shot + pipeline durable pour les artistes hors-app ; enrichissement MusicBrainz automatique.

**Quoi** : ① **Profil** : ratings → Liked MVs → Followed groups ; picker bias/favorite = liste COMPLÈTE (le cap `.slice(0,60)` coupait à « Chanelle Moon », rang 60/841 exact — prouvé SQL) en rendu incrémental (sentinelle IntersectionObserver), vignettes croppées (le bucket `member-photos` contournait le faceCrop d'Avatar → 60 images pleine résolution), useDeferredValue. ② **/admin/debuts** ne crashe plus (payload minimal `{reason}` des rows auto-écartées déréférencé sans garde — row BOYNEXTDOOR dans la fenêtre ; type union honnête + affichage compact du reason) + section « Lineup unmatched ». ③ **Recherche header** : les 32 solistes n'apparaissent plus en double — le segment groupes résout le membre canonique (href /artists/, photo self-host) et déduplique le segment Artist. ④ **Romanisation** (renversement assumé) : quand la chanson est hangul avec glose latine, la glose seule s'affiche (« NEMONEMO », ~600 MVs) ; garde-fous feat./sans-glose/déjà-latin ; 3 canoniques Yena renommés au format glosé en prod (le cron INSERT-only ne réécrit jamais un titre). ⑤ **Pipeline hors-app durable** : migration 0059 `lineup_unmatched` (deny-all RLS) + collecteur anti-bruit dans scrape-music-shows (persistance best-effort) + file admin avec Create-via-fandom/Ignore. ⑥ **MusicBrainz dans createFromPayload** : liens officiels (13 clés LinksBar par domaine) + membres/birthdays (noms MB souvent hangul → match par sort-name ; throttle 1,1 s ; jamais d'enrichissement sur match ambigu). ⑦ **One-shot Lot M** : 15/18 récurrents créés (TRENDZ avec « On My Knees », GIRLSET, idntt, UAU, IDID, ASCENDER, USPEER, ANTARES, OMEGA X, CLASSy, I.MET.U, AtHeart, UDTT + solistes Jang Haneum, Baek A Yeon) — dossiers complets vérifiés SQL (image self-host 15/15, jusqu'à 9 liens MB, birthdays où MB les a, ~50 MVs backfillés) ; discovery ciblée → chaînes TRENDZ/OMEGA X/UAU (compagnie Dreamcatcher) mappées + promotion monitored ; échecs fandom (Hat:q, GABEE, Ayumu Imazu) et ASCENDER → file durable/cron lundi. ⑧ **Page épisode** (branche) : table `show_episodes` (row stable par show+jour KST, backfill 196 rows) + `comments.episode_id` (check exactement-une-cible), commentaires généralisés (insert MV tolérant à la migration), route `/show/[show]/[day]` (logo, n°, lineup, stages, discussion), routage music_show → page épisode (évolution ledger « stage links »), grouping : les rows à stage_url fusionnent désormais.

**Vérification** : 722 tests verts, CI verte sur les 6 merges (db-repro a validé 0059 sur base fraîche), sondes Playwright prod : recherche « yena » → 1 profil /artists/yena ✓, page Yena → NEMONEMO/Hate Rodrigo sans hangul ✓.

**Restes** : appliquer 0059 + 0060 en prod (MCP down) puis merger `feat/show-episode-page`, dispatch scrape (upsert épisodes) et vérifier /show/inkigayo/2026-07-19 ; membres idntt à élaguer (20 — l'infobox fandom a sur-capté) ; sweep --thin à poursuivre (~28 groupes ≤ 1 MV restants).

## 2026-07-17 — Round de retours Rudy : 7 lots (lineups, notifs, UI, couverture MV)

**Branche/commit** : 7 merges → `main` : `fix/aggregator-future-coverage` (`7448f2d`), `fix/ui-quick-wins` (`0d7a4f7`), `feat/comment-rail-v3` (`f6baa0f`), `feat/lineup-full-calendar` (`6e0906a`), `feat/groups-client-tabs` (`24307d4`), `feat/notif-wording-schedule` (`098847d`), `chore/mv-coverage-sweep` (`f0f9b47`). Plan approuvé (4 décisions actées avec Rudy : notifs post-scan, titre event-led, histogramme demi-barres, lineup complet au calendrier).

**Quoi** : ① **Aggregator music-shows** : `wellCovered` exige un lineup daté d'aujourd'hui-KST+ — le carrd périmé (épisode passé) masquait le board SBS qui publiait déjà l'Inkigayo 1318 du 19/07 ; sources injectables (les tests exercent la vraie logique, le helper dupliqué divergeait depuis le 12/06). **Vérifié prod** : ép. 1318 ingéré via fallback `sbs-inkigayo` (6 groupes matchés) — 1er run silencieusement vide (fetch Jina transitoire, ni erreur ni usage loggés), 2ᵉ run OK. ② **Histogramme notes** : 20 demi-barres (un 8.5 a SA barre — `Math.ceil` le comptait dans le 9) ; footer MV « dropped 1mo ago · date » → date seule. ③ **Fil commentaires v3** : rail continu pleine hauteur = exactement la zone repliable (le tronc v2 disparaissait pour une racine à 1 réponse, cas par défaut). ④ **Lineup** : « A, B, C & 7 more » (fini le « +7 ») partout ; liste de jour du calendrier = lineup COMPLET multi-ligne (`lineupDisplay="full"`). ⑤ **Groups/Solo 100 % client** : les 2 jeux préchargés, switch `useState` + `replaceState`, zéro requête au clic (vérifié `next start` local), deep-link `?tab=solo` conservé. ⑥ **Notifs** : titre daily event-led (« aespa — KISS N TELL · today », l'OS affiche déjà « from KStage »), body étiqueté par jour dans le fuseau de l'abonné, horaires déplacés APRÈS le scan du soir (digest 10:30 UTC, alertes 10:45, copy day_of « Out now »), bloc explicatif des 2 flux dans /account (`DIGEST_SEND_UTC` miroir de crons.yml). ⑦ **Couverture MV** : YENA + Stone Music (Hate Rodrigo + 4 MVs d'ère récupérés, 3 doublons cross-chaîne + 1 « MV 톺아보기 » masqués) ; mode `--thin` du script discovery (33 groupes ≤ 1 MV) — 82MAJOR et 8TURN mappés/backfillés (0→8 MVs chacun) ; cron discover-channels élargi à tout catalogue fin + rotation hebdo déterministe.

**Pièges appris** : le browser pane ne révèle jamais le contenu streamé (`loading.tsx` = boundary Suspense) des pages localhost — dev ET `next start`, vieux code inclus ; discriminant : /mv (sans loading.tsx) marche. Vérification interactive → retirer temporairement le boundary et rebuilder, ou tester la prod déployée. PowerShell mange la virgule de `--slugs=a,b` (quoter). Une source seedée sans backfill profond peut rester à 0 MV (le cron ne pagine que la tête de playlist).

**Restes** : 31 groupes ≤ 1 MV à balayer (`--thin --limit=20`, ~4 100 unités/passe) ; matching hangul-only (« 선미 » ne matche pas Sunmi dans les lineups SBS) ; fetch broadcaster silencieusement vide (pas d'erreur loggée) — les deux consignés au BACKLOG.

## 2026-07-17 — Nuit de vérification : timezone anniversaires, favicon.ico, lisibilité des push

**Branche/commit** : `fix/viewer-tz-coherence` (merge `1278bbc`), `fix/favicon-ico` (merge `9b1e06d`), `fix/notif-readability` (merge `1fa9d5c`) → `main`. Demande Rudy (nuit) : vérifier ① le décalage D-1 des anniversaires vs D-DAY des music shows, ② la cohérence des logos sur toutes les surfaces, ③ la lisibilité des notifs.

**① Timezone (bug confirmé en prod, vue Europe/Paris)** : la grille du calendrier groupait en fuseau viewer pendant que les rangées `QueueRow` retombaient sur le **défaut KST** (prop `timeZone` non passée — calendrier, fiche groupe, search, landing) → « Music Bank D-DAY » et « Taemin — 33 D-1 » dans la **même cellule du jour**. Cause profonde double : (a) défaut `'Asia/Seoul'` silencieux sur QueueRow/NextDropCard, (b) les **anniversaires sont des dates pures** ancrées à minuit KST puis day-keyées comme des instants → glissement à J-1 partout à l'ouest de Séoul (Wonwoo 17/07 rangé sous le 16). Fix : `eventDayKey`/`eventDDay`/`groupEventsByEventDay` (anniversary = date civile lue en KST, le reste = jour local du viewer), `timeZone` **requis** (le compilateur attrape les oublis futurs — c'est le mécanisme, pas le symptôme), rangées anniversaire sans heure ni countdown, ticker en fuseau viewer, fenêtre des anniversaires calée sur le jour civil du viewer. **Décision sémantique** : un anniversaire s'affiche sur sa **date civile** dans tous les fuseaux (aligné iCal all-day + ledger « birthday = date pure ») — PAS sur l'instant du flip minuit-KST converti (qui aurait mis Taemin 18/07 en D-DAY le 17 à Paris et Wonwoo sous le 16). Vérification : cas réels Wonwoo/Taemin en tests (date.test, calendar-month.test), 678 tests + build verts, prod re-curlée après déploiement.

**② Logos (audit agent, 15 surfaces)** : identité cohérente par construction (tout est régénéré du même markup K par `generate-icons.mjs`, badge Android bien monochrome, manifest/OG/apple-icon 200). **Un seul vrai défaut : `/favicon.ico` 404** (Safari macOS ignore les favicons SVG ; crawlers/unfurlers GET en aveugle) → cible ICO 16/32/48 (entrées PNG) ajoutée au générateur, `src/app/favicon.ico` committé. Mineurs consignés sans action : `purpose "any maskable"` combiné, `themeColor` figé sombre, pas de splash iOS.

**③ Notifs (audit agent + fixes)** : `send-digest` passait le **titre brut** (« fromis_9 — fromis_9 2nd Album – Glow ME (2026) ») → `displayEventTitle` comme le flux comeback ; `sendPush` sans TTL = défaut web-push **4 semaines** → un « Today: … » périmé pouvait arriver des jours après → `TTL 72 000 s (20 h)` + `urgency: normal` ; titre daily « Today in k-pop » mensonger (fenêtre 48 h) → « **Today & tomorrow in k-pop** ». Restes consignés au backlog (non codés cette nuit, décisions produit) : toggle « Birthdays & anniversaries » **mort** (aucun event anniversary ne passe par la table `events` que lisent les crons), capture du fuseau à l'abonnement (l'unique abonné réel a `timezone NULL` → push en jour KST), body comeback type-aware, jours dans le weekly digest.

**Branche/commit** : `feat/monitoring` (merge `297391c`) → `main`. Décision Rudy : alertes via **GH Actions (email natif) + carte admin**. **Quoi** : ① Module pur `src/lib/monitoring/health.ts` (8 tests) — contrat d'alerte de l'audit §6.3 : 2 runs consécutifs non-`ok` d'une même source ; music show à J-1 sans données (**promotion** de `details.stale_alerts` déjà calculé par le scraper — pas de ré-implémentation) ; source critique sans run `ok` depuis 2 × sa cadence **ou jamais loguée** (couvre le cron mort et le `logScrapeRun` silencieusement cassé). Cadences : music_shows 12 h, le reste 24 h ; wikipedia/fandom_debuts/images non-critiques (règle 1 seule). ② Route `GET /api/cron/monitor` (CRON_SECRET, **read-only**) : alerte → 500 → `exit 1` du step curl → run GH Actions rouge → email — le trou « `partial` = HTTP 200 = run vert = invisible » est fermé. Échec du monitor lui-même → 500 aussi. Anti-spam : pas de dédup (1 run/jour = 1 email/jour max par problème persistant). ③ Schedule `30 13 * * *` (22:30 KST, après le dernier scrape du jour) + option `workflow_dispatch`. ④ Carte **« Source health »** dans `/admin` (`getSourceHealth`, `requireAdmin` + service role — même évaluation que le monitor) : chip statut, dernier run, alertes par famille = la **fraîcheur par famille** de l'audit §7.6. ⑤ Le « last updated » global de la landing n'était plus affiché → champ mort `lastScrapedAt` retiré de `getSourcesStatus` (count via `head: true`). **Vérification** : typecheck + lint + prettier + **615 tests** + `next build` verts ; route appelée en local contre la prod (read-only) → **500 avec 2 alertes VRAIES** : `notify_comebacks`/`send_digest` jamais logués — normal, leur logging arrive avec le Lot 4 déployé aujourd'hui et leurs crons ne tournent que demain matin. **Bootstrap assumé** : le run monitor d'aujourd'hui 13:30 UTC enverra UN email GitHub, se résorbe dès les runs de demain. **Phase 1 (contrat de confiance) : close** — gate de sortie couvert : tz multi-fuseaux (3a/3a-polish/3b), zéro faux push + idempotence (Lot 4), alertes sur error/partial répétés (Lot 5), aucune classe de faux event éligible au push (hidden/cancelled filtrés + contrat tentative testé).

## 2026-07-16 — Phase 2 Lot 5 : consolidation typographique légère — **Phase 2 close**

**Branche/commit** : `feat/typo-light` (`76b6de4`, merge `9109bf6`) → `main`, **mergé après review visuelle Rudy sur la preview Vercel de la branche** (process bloquant respecté). **Quoi** : consolidation des tailles arbitraires **~20 → 10 niveaux** (audit §8.2), familles intouchées (décision Rudy). Mapping mécanique : plancher micro-labels **9 px** (7/7.5/8/8.5 → 9), 9.5 → 10 (dont `.label-data` en CSS), 10.5 → 11, 11.5 → 12, 12.5 → 13, 15 → 14, 30/34 → 32 ; conservés : 9/10/11/12/13/14/17/21/32/150 (artist-hero décoratif). 21 fichiers, ±35 lignes. **Vérification** : 648 tests + build verts ; review preview Rudy OK ; CI verte par merge ; re-run Lighthouse post-Phase 2 consigné dans `docs/PERFORMANCE_AUDIT.md`.

**Phase 2 (activation / simplification UX / analytics) : CLOSE.** Les 12 actions de l'audit §12 livrées en 5 lots : analytics first-party + dashboard + baseline perf (Lot 1), funnel d'activation orienté calendrier (Lot 2), UX mobile + fallbacks explicites + labels honnêtes (Lot 3), passe a11y (Lot 4), typo (Lot 5). Gate de sortie « un testeur atteint un calendrier personnalisé sans explication en < 2 min » : le parcours est raccourci (CTA 2ᵉ écran, recherche onboarding, « See your calendar ») et **instrumenté** (`signup_completed` → `calendar_opened` dans `product_events`) — la mesure réelle viendra des testeurs de la Phase 4. Prochaine étape : **Phase 3** (tiers de confiance Vérifié/Surveillé/Candidat + reproductibilité DB §9.2).

## 2026-07-16 — Phase 2 Lot 4 : passe d'accessibilité (finition §8.6)

**Branche/commit** : `feat/a11y-pass` (`34909f5`, merge `63ed15d`) → `main`. **Quoi** : ① **Skip-link** premier focus → `<main id="main" tabIndex={-1}>` (WCAG 2.4.1). ② **`PanelHeader` rend un vrai heading** (`as='h2'` défaut) — navigation par sections au lecteur d'écran, aspect strictement identique (preflight neutralise les styles h\*) ; **h1 sr-only sur la home connectée** (`/calendar` en avait déjà un dans `calendar-month.tsx:111` — l'affirmation « pas de h1 » du plan était périmée, vérifiée avant d'en créer un doublon strict-mode). ③ **`:focus-visible` global** (outline 2px `--ring`, couche base — les `outline-none`+ring existants gagnent) + ring sur l'input URL du feed iCal (seul vrai trou trouvé au grep). ④ **Ticker** : répétitions du marquee `aria-hidden` — une annonce n'est plus lue jusqu'à 8×. ⑤ **`prefers-reduced-motion` étendu à `.animate-pulse`** (skeletons). ⑥ **Contrastes thème clair vérifiés par calcul** (conversion oklch→sRGB→WCAG en script) : tout ≥ 4.85:1 sur carte blanche ✓, mais `--faint` 4.12:1 et `--teal` 4.18:1 **sur `--secondary`** → abaissés (L 0.55→0.52 / 0.53→0.51), ≥ 4.5:1 partout en usage texte. ⑦ **Histogramme de notes** : `role="img"` + liste `sr-only` par bucket, barres décoratives masquées. ⑧ **Cibles tactiles** : utilitaire `.tap-target` (extension verticale ::after en `pointer: coarse` — pas horizontale, elle mordrait les chips voisines) sur toggles mv-chart + pills drops ; chips calendar `min-h-6` (≥ 24 px, WCAG 2.5.8 AA — leur conteneur `overflow-x-auto` clipperait un ::after) ; pause ticker 32×30 = AA. Bottom-nav tardive au clavier : couverte par le skip-link, pas de déplacement DOM. **Vérification** : **648 tests** (5 nouveaux : PanelHeader heading ×3, ticker SR ×2) + build verts ; **26/26 E2E** smoke/search/feedback en mode CI local.

## 2026-07-16 — Phase 2 Lot 3 : UX mobile, fallbacks explicites, labels honnêtes (+ fix CI e2e)

**Branche/commit** : `feat/mobile-ux-labels` (`6babc4e`, merge `e069f00`) → `main`. **Quoi** : ① **Filtres avant la grille en mobile** (§8.5) : `MobileGroupFilter` (`lg:hidden`, chips My groups/All + disclosure contenant le `GroupFilter` existant) inséré avant les chips de `/calendar`, branché sur le **même** `CalendarFilterProvider` (zéro refetch) ; bloc Filters de la sidebar → `hidden lg:block`. _Ledger : la rangée scope avait été retirée (doublon desktop) — réintroduction mobile-only, là où la sidebar est sous la grille._ ② **Recherche header mobile** : `h-9 text-sm` + wordmark masqué < 380 px (l'input /search était déjà à 16 px depuis R8). ③ **Fallbacks globaux explicites** (§8.4) : prop `note` sur `PanelHeader` → « All groups » sur queue/This week en repli global, « Includes all groups » sur Fresh drops complété. ④ **Labels** (§8.7) : « Trending » → « **In the spotlight** » (le tri mesure l'imminence, pas l'audience) ; **Top rated** : `minCount` 1→2 + **nombre de votes `(n)`** affiché après le score (cumul avec la date R6, conflit ledger résolu). ⚠️ **Réalité prod vérifiée avant merge** : 0 event n'a ≥ 2 votes (6 × 1 vote) → le panel Top rated **disparaît** jusqu'aux premiers votes croisés — assumé (un chart à 1 vote est mensonger), réversible en un paramètre. ⑤ **Fix CI e2e** (rouge depuis le merge du Lot 2) : le double CTA landing violait le **strict mode Playwright** (`getByRole` → 2 éléments) → `.first()` sur les 3 sélecteurs de `smoke.spec.ts`. Reproduit et validé en local **mode CI** (`CI=1`, build prod servi par `next start`, 8/8 desktop+mobile — leçon `e2e_prod_build_ci` appliquée ; auth.spec jamais lancé en local, compte réel). **Vérification** : typecheck + lint + prettier + **643 tests** + build verts ; classe de sélecteurs auditée sur les 4 specs.

## 2026-07-16 — Phase 2 Lot 2 : funnel d'activation orienté calendrier (landing, onboarding, email)

**Branche/commit** : `feat/activation-funnel` (`f8ba40a`, merge `23e7f05`) → `main`. **Quoi** : ① **Landing réordonnée** (audit §8.3, aucune section redesignée) : promesse → preuve courte → **CTA au 2ᵉ écran mobile** (il arrivait plusieurs écrans sous la ligne de flottaison) → exploration (mur, previewRows déplacées dans « **More coming up** » avec action `Full calendar →`, 3 étapes, proof bar) → CTA répété en bas. Label « Happening on KStage right now » → « **Next up on KStage** » (l'event affiché est futur — §8.7) ; dot live→teal ; copie des 3 étapes alignée sur le réel (« Day before and day of » — le push announced n'existe plus). ② **Onboarding** (§12 action 3 « trouver SES trois artistes ») : liste complète (~140 groupes, top 30 affiché par défaut) + **recherche client** (`filterGroups` pure, casse/accents pliés), objectif « **Pick at least 3** » + compteur jamais bloquant, **étape finale « Your calendar is ready »** avec CTA primaire « See your calendar » → `/calendar` (décision Rudy : la home reste la sortie secondaire) — le toast est remplacé par cet écran qui cape la promesse. ③ **Email de bienvenue** : CTA principal « Open your calendar » → `/calendar`, liens `/groups` + `/account` (notifications). **Vérification** : ordre DOM SSR strict vérifié par curl (promesse < preuve < CTA < mur < More coming up < proof < CTA bas) ; **652 tests** verts dont 10 nouveaux (séquencement grid→notifs→done→/calendar en jsdom, recherche au-delà du top 30, skip, filterGroups, contrat welcomeHtml) + build. Le parcours signup→calendrier est maintenant **mesuré** par les events du Lot 1 (`signup_completed` → `calendar_opened`). **Pièges** : cache Turbopack `.next/dev` périmé après un gros diff (« Cta is not defined » alors que build+typecheck verts) → purge `.next/dev` + restart ; RTL matche le nom accessible COMPLET (≠ Playwright substring) mais une regex non ancrée `/Group 1/` matche « Group 10 » ; jsdom sans `matchMedia` → shim pour IosInstallHint ; **`beforeEach(() => mock.mockReset())` sans accolades = teardown fantôme** (le mock retourné est rappelé après le test).

## 2026-07-17 — Phase 3 Lot 4 : provenance des fallbacks, fusion des annonces, Jina durci, chaînes impaginables

**Branche/commit** : `feat/scraper-robustness` (`5c762b8`, merge `29a96fb`) → `main`. **Quoi** : ① Migration **0058** : 6 rows sources broadcasters versionnées ; `SourceScraper.sourceUrl` ajouté ; la route music-shows pose **`source_id` = le provider réel du lineup** (insert + time-shift), `source_url` reste l'URL carrd (clé d'idempotence, leçon 0040). Vérifié : la route charge le carrd par URL, pas par type — les 7 rows `music_shows` ne cassent pas le `maybeSingle`. ② **Fusion des annonces plus précises** : `resolveNearDup` pur — un candidat `confirmed` (kpopofficial, heure exacte) **promeut** la `tentative` existante (wikipedia, minuit KST) au lieu de la jeter (UPDATE start_at/status/image-si-absente, `source_url` intouché, compteur `upgraded`, idempotent au 2ᵉ run). ③ **Jina durci** (minimal honnête — un fallback fetch-direct exigerait de réécrire 5 parsers markdown) : `fetchViaJina` partagé, timeout 25 s + 1 retry, clé `JINA_API_KEY` **optionnelle** (décision budget Rudy), erreurs préfixées `jina:` pour le monitor. ④ **Chaînes impaginables** (SUCTION/1theK > 20k uploads) : `scrapeGroup` accepte `opts.uploads` (injection, 2 lignes) + `scripts/backfill-mv-search.ts` (search.list ciblé → LE pipeline existant, zéro duplication) — one-shot manuel, jamais en cron ; SCRAPING §7 coché. **Vérification** : 671 tests (5 resolveNearDup) + build verts ; 7 rows music_shows en prod ; le run scrape-music-shows suivant exerce la provenance naturellement.

## 2026-07-17 — Phase 3 Lot 3 : découverte de chaînes hebdo + création récupérable + gate testé

**Branche/commit** : `feat/candidate-pipeline` (`6f2be23`, merge `3ba19b1`) → `main`. **Quoi** : ① Lib `channel-discovery.ts` (le one-shot devient un wrapper CLI) : `filterMvSearchHits` pur, `discoverChannelsForGroup` (~205 unités), `seedAndBackfillChannel` — **garde ≥ 2 MVs title-matchés** pour le seed auto (1 seul = revue, jamais seedé), **promotion candidate → monitored**, backfill immédiat. ② Cron **hebdo** `discover-channels` (lundi 11:00 UTC) : cibles = groupes < 3 MVs visibles ET (debut < 180 j OU candidate — le chemin de sortie de quarantaine), budget 3 groupes/run (~650 unités/10k quota), monitoring 168 h non-critique. ③ **Création récupérable** (pas de RPC : Storage non-transactionnable, dérive TS↔SQL, et le bouton admin devient retry=resume gratuitement) : `createFromPayload` reprend une création interrompue (resume par nom, membres par différence, source tolérante, event idempotent), `stepErrors` remontées en scrape_log. ④ Score/quarantaine minimal : `debutGateDecision` pur extrait + **`deezerFans` persisté** dans le payload (la revue admin voit le signal). ⑤ **GATE DE SORTIE Phase 3 testé dans les deux sens** : fiable → `{autoCreate, monitored}` + MV youtube_api notifiable sans intervention ; ambigu → pending, et `candidate` n'émet jamais rien des 2 builders. La garde `events-hidden` a attrapé un count sans filtre (corrigé — compter des MVs cachés fausserait « catalogue fin »). **Vérification** : 666 tests + build verts, CI verte.

## 2026-07-17 — Phase 3 Lot 2 : tiers de confiance internes + gates (notifs, sitemap, noindex)

**Branche/commit** : `feat/confidence-tiers` (`6dbbe5e`, merge `1a909ae`) → `main`. Décision Rudy : tiers **internes** (admin seulement). **Quoi** : ① Migration **0057** : enum `group_confidence` (`verified`/`monitored`/`candidate`) + `groups.confidence` — **backfill ajusté après pré-check prod** : les 5 groupes sans chaîne (Cherish, DK X Seungkwan, NCT JNJM, SUCTION, V8) ont roster complet + agence connue = des « Surveillés » légitimes, pas des ambigus → **`candidate` démarre vide** et se peuplera par les futures auto-créations (distribution : 166 verified / 5 monitored / 0 candidate). ② **Gate notifications** dans les builders purs (`passesConfidenceGate` partagé) : `candidate` jamais ; `monitored` seulement `confirmed` OU source `youtube_api` ; absent = historique — + filtre défensif côté routes. ③ **Gate SEO** : sitemap exclut les candidate (groupes + MVs) et **corrige l'incohérence pre-debut historique** (noindexé mais sitemappé) ; noindex sur `groups/[slug]` et `/mv/[slug]` candidate. ④ Auto-création (`debuts/ingest`) : `monitored` si chaîne vérifiée, sinon `candidate` — jamais `verified` d'emblée. ⑤ Admin : répartition des tiers + chip sur `/admin/debuts`. **Vérification** : 656 tests (13 tests contrat confidence sur les 2 builders) + build verts ; distribution prod vérifiée par SQL. **Rétro-fix embarqué** : 0044/0050 inséraient des members avec des **UUID groups de prod** → violation FK sur base fraîche (découvert par le job CI DB, diagnostic auto-posté) → jointure sur `groups` (no-op propre sur base vierge, prod inchangée).

## 2026-07-17 — Phase 3 Lot 1 : base fraîche reproductible + job CI DB

**Branche/commit** : `feat/db-repro` (`2d3fdba`, merge `5d0d011` + hotfix `f02be28`) → `main`. **Quoi** : ① Migration **0055** : index unique **partiel** `sources(url) where group_id is null` — depuis 0033, `unique(url, group_id)` ne dédupliquait plus les sources globales (NULL ≠ NULL) ; pré-check prod : 0 doublon. ② Migration **0056** : rows sources **structurelles** versionnées (kpopofficial, community, wikipedia) en **miroir exact des rows prod** — découverte au pré-check : elles existaient déjà en prod (insérées à la main via MCP, jamais versionnées), le scraper Wikipedia tournait donc déjà ; 0056 versionne l'existant, une base fraîche ne perd plus le fallback anti-SPOF (audit §9.2). ③ `seed.sql` réparé : les 10 `on conflict (url)` morts → `(url, group_id)` ; blocs structurels retirés (migrés en 0056). ④ `supabase/config.toml` + workflow **`.github/workflows/db.yml`** (path-filter `supabase/**`) : `supabase db start` + `db reset` = rejoue migrations + seed sur un Postgres jetable. **Le job a fait son travail dès son 1ᵉʳ run** : rouge sur les UUID prod de 0044 (la classe « migrations de contenu » que l'audit annonçait). **Innovation d'outillage** : les logs Actions étant illisibles sans auth (403, pas de gh CLI), le job poste le tail de son log en **commit comment** (API publique) — diagnostic autonome. CLI épinglée 2.109.1 (= génératrice du config.toml). **Vérification** : migrations appliquées prod (no-op vérifié : 1 row par type), CI verte ; le job DB vert est attendu au merge du Lot 2 (rétro-fixes 0044/0050 embarqués).

## 2026-07-16 — Phase 2 Lot 1 : analytics produit first-party + baseline perf

**Branche/commit** : `feat/analytics-baseline` (`439b982`, merge `a1068e0`) → `main`. Ouvre la **Phase 2 (activation/UX/analytics)**. Décision Rudy : **table Supabase first-party** (pas de PostHog ni Vercel Pro — les custom events Vercel exigent le plan payant). **Quoi** : ① Migration **0054 `product_events`** (17 events en CHECK fermé, `user_id` FK `set null` = suppression de compte → anonymisation sans perdre les agrégats, `anon_id` cookie first-party httpOnly `kstage_aid`, **ni IP ni UA**, dédup `day_key` once/daily par index unique partiel, RLS deny-all) — appliquée en prod, dédup vérifiée par SQL (2e insert → 0 row). ② Lib : vocabulaire fermé + `CLIENT_ALLOWED_EVENTS` (un client ne fabrique pas un `signup_completed`), `sanitizeProps` whitelist par event, `trackEvent` best-effort **no-throw** (l'analytics ne casse jamais un parcours). ③ **`POST /api/e`** (nom court : `/api/track` matche les listes adblock) : 204 systématique, rate-limit RPC connecté / cap 60/h par `anon_id` anonyme — vérifié en local : props `evil` filtrée, event interdit = 0 row, cookie posé. ④ Instrumentation des 17 events de l'audit §10.3 — la **north-star `calendar_opened`** (home connectée OU `/calendar`, follows ≥ 1, dédup 1/jour) passe par un composant client `TrackView` monté (jamais de side-effect RSC : le prefetch des `<Link>` gonflerait la mesure) ; « notification envoyée » lue des tables existantes (pas de double source). ⑤ `?src=push` sur les URLs de notification + **`sw.js` matche par pathname** (l'`includes()` d'avant ne re-focusait plus un onglet à l'URL nettoyée). ⑥ Carte **« Activation »** dans `/admin` : funnel 7/30 j, série north-star hebdo, recherches vides, notifs envoyées. ⑦ **Baseline Lighthouse prod** (`docs/PERFORMANCE_AUDIT.md`) : landing 62 / calendar 73 / mvs 54 (mobile) — **CLS 0.249 identique sur les 3 pages = cause partagée** à fixer post-Lot 5. **Vérification** : typecheck + lint + prettier + **632 tests** + build verts ; endpoint et dédup vérifiés contre la prod. **Piège vitest débusqué** (bissection) : `beforeEach(() => mock.mockReset())` sans accolades RETOURNE le mock → vitest l'exécute comme teardown → le mock est rappelé avec l'implémentation du test (un throw devient l'échec du test, attribué à la mauvaise ligne). Classe absente ailleurs (grep).

## 2026-07-16 — Phase 1 Lot 3b : push comeback dans le fuseau de chaque abonné

**Branche/commit** : `feat/notif-per-tz` (merge `410bb0a`) → `main`. Décision Rudy : **labels par fuseau, heures d'envoi inchangées** (l'envoi « matin local » via cron horaire reste une option post-bêta). **Quoi** : ① `resolveKind(event, now, timeZone)` calcule Today/Tomorrow via `localDayKey` dans le fuseau de l'abonné — « 🔥 Today » est enfin vrai chez lui. ② **Piège désamorcé** : `addDaysKey` repassait par `kstDayKey` ; porté naïvement à un fuseau négatif, le round-trip minuit-UTC rendait J-1 → réécrit en arithmétique pure sur la clé. ③ `buildComebackNotifications` : 7e param optionnel `timeZones` (map userId→IANA — propriété du user, pas du device ; défaut KST = comportement historique, fixtures intactes). Un même event peut être `day_of` chez l'un et `day_before` chez l'autre (voulu) ; la dédup `userId:eventId:kind` garantit toujours ≤ 2 push/event/user. ④ Cron : jointure `profiles.timezone` (validée `isValidTimeZone`), fenêtre `[now-1j, now+3j)` conservée avec preuve de couverture UTC-12→UTC+14 en commentaire. Abonnés sans réglage → KST (le cookie anonyme est hors de portée d'un cron — assumé, le réglage existe dans /account). ⑤ **Digest inchangé par décision** : son payload ne cite aucun libellé de jour (vérifié `digestLabels`), le titre est générique — rien à fuseauter. **Vérification** : typecheck + lint + prettier + **607 tests** (3 nouveaux : Séoul `day_before` vs LA `day_of` sur le même event, changement de mois en fuseau négatif, map vide = KST) + `next build` verts ; pas de déclenchement manuel du cron d'envoi (leçon `verify-automation-state-first`) — la logique jour/fuseau est entièrement unit-testée, la jointure est typecheckée.

## 2026-07-16 — Phase 1 Lot 4 : budget de notifications (announced coupé, digest idempotent, échecs comptés)

**Branche/commit** : `feat/notif-budget` (`ff511f2`, merge `8499dd4`) → `main`. Décisions Rudy : **couper le kind `announced`** (option la plus sobre) ; **pas de toggle par canal** (un seul canal push existe — YAGNI, le schéma `user_notification_settings.channel` reste prêt). **Quoi** : ① `announced` supprimé de `comebacks.ts` (`NotificationKind` = `day_before | day_of`, payload 🎉 retiré, champ `createdAt` mort supprimé) + la 2e requête events du cron (`created_at > now-24h`) — l'annonce vit dans le digest quotidien, le push est réservé aux échéances (audit §7.3 : jusqu'à 3 push/comeback + digests = trop). Les rows `event_notifications kind='announced'` restent (historique). ② **Idempotence digest** : migration **0053 `digest_log`** (`user_id, day_key UTC, edition, unique` — RLS deny-all même migration, pattern 0030/0024 ; appliquée en prod via MCP + types régénérés, vérifiée : RLS on + 1 policy). Granularité par **user** : un re-run ne renvoie qu'aux non-servis ; multi-device = 1 ligne ; `ignoreDuplicates` couvre la course inter-runs. Le builder reste pur (filtrage dans la route), `skipped` exposé dans le JSON. ③ **Échecs comptés** : `failed` (retour `sendPush`) dans les 2 routes + `logScrapeRun` sources `notify_comebacks`/`send_digest` (`ok`/`partial`/`error`, 500 si tout échoue → run GH Actions rouge → email) — c'est ce que lira le monitor du Lot 5. ④ **Tentative** : `events.status` projeté dans les 2 pipelines + politique documentée dans `comebacks.ts` (jour connu ⇒ day_before/day_of légitimes ; tout futur kind à heure précise devra exclure `isTimeTBA`, audit §7.5) + test-contrat (day_of émis, aucun payload ne cite une heure). **Vérification** : typecheck + lint + prettier + **604 tests** (3 tests announced retirés, 2 ajoutés) + `next build` verts ; table prod vérifiée par SQL ; **pas de déclenchement manuel du cron d'envoi** (leçon `verify-automation-state-first` : jamais tester un cron d'envoi réel). CI verte par merge.

## 2026-07-16 — Phase 1 Lot 3a-polish : dernières surfaces KST → fuseau du viewer

**Branche/commit** : `feat/tz-polish` (`320abbe`, merge `89ed423`) → `main`. **Quoi** : les sites d'affichage encore en `'Asia/Seoul'` dur passent au fuseau du viewer (prop `timeZone` **obligatoire**, résolue par `getViewerTimeZone()` dans chaque page — pas de défaut silencieux). ① Puces D-day `group-card` (relayé par `groups-grid`) + `trending.ts` (`trendingReason`/`pickTrending` param `timeZone`) depuis `/groups` et `/u/[username]`. ② Mois-année `mv-card` — parents traités en **classe entière** (l'explo en a trouvé 4 de plus que prévu) : `mvs-grid`, `collapsible-mvs`, `drops-grid`, `fresh-drops`, + rendus directs `groups/[slug]`, `artists/[slug]`, `mv/[slug]`. ③ `mv-chart` (Top rated, client → prop depuis `/mvs`). ④ Hero « Latest drop » `/mvs` (`formatKst` → `shortDate(…, tz)`, uppercase déjà porté par la CSS `label-data-inline`) et ligne « dropped » de `/mv/[slug]`. ⑤ **Birthday** : `members.birthday` est une date calendaire pure → format **UTC**, pas le fuseau viewer (un fuseau négatif afficherait la veille ; `'Asia/Seoul'` ne marchait que par chance). **Pourquoi** : reste cosmétique du Lot 3a — cohérence « in your timezone » sur toutes les surfaces. **Vérification** : SSR diffé par curl avec `tz=America/Los_Angeles` vs `Pacific/Kiritimati` → les D-day des events proches de minuit bougent d'un jour (D-7↔D-8, D-12↔D-13), les autres stables ; cookie anonyme toujours posé sans boucle ; test trending multi-fuseaux (D-1 Séoul = D-DAY Los Angeles) ; typecheck + lint + prettier + **605 tests** + `next build` verts. **À ne pas toucher confirmé** : ticker KST, `moderation-list` (admin, libellé « KST »), ingestion/idempotence scrapers. **Piège rencontré** : le lint `react-hooks/purity` interdit `Date.now()` dans un render RSC → passer `undefined` et laisser le défaut de la lib.

## 2026-07-15 — Phase 1 Lot 3a : timezone par viewer (réglage + affichage)

**Branche/commit** : `feat/viewer-timezone` (`1bec8a2`) → `main`. Décisions Rudy : anonyme via **cookie navigateur** ; ticker gardé **KST**. `profiles.timezone` était **morte** (jamais écrite). **Quoi** : ① `getViewerTimeZone()` mémoïsé (`src/lib/profiles/timezone.ts`) : profil → cookie `tz` → défaut KST, avec `isValidTimeZone`. ② Réglage : `<select>` IANA dans Account + `updateProfile` écrit `profiles.timezone` (validé). ③ Anonyme : `TimezoneCookie` (Client Component au layout) détecte le fuseau navigateur, pose le cookie `tz` + **1 seul** `router.refresh` (garde anti-boucle) → « in your timezone » tenu sans compte. ④ Threading du fuseau résolu vers home (hero + queue/week), **calendrier** (`calendar-month` via `CalendarEvents`), `groups/[slug]` ; ticker reste KST explicite. Les primitifs `date.ts` (`localDayKey`/`formatDDay`/`groupEventsByDay`) acceptaient déjà un `timeZone`. **Pourquoi** : tenir la promesse « in your timezone » de la landing (exigence lancement international). L'heure PRIMAIRE était déjà correcte via `LocalTime` (navigateur) ; ce lot corrige D-day, regroupement par jour et calendrier côté serveur. **Vérification** : cookie `tz=Europe/Paris` posé en preview (anonyme), **pas de boucle**, 0 erreur console ; formatage tz-aware unit-testé (`formatDDay` Paris vs Seoul) ; threading typecheck-enforcé (props requises) ; 604 tests + build verts. **Différé** (lot polish, cosmétique) : puces D-day `group-card`/`trending`, mois-année `mv-card`/`mv-chart`, birthday. **Reste** : Lot 3b (push/digest par abonné), Lots 4-5.

## 2026-07-15 — Phase 1 Lots 1-2 : `hidden` filtré partout + dates `tentative` honnêtes

**Branche/commit** : `fix/hidden-everywhere` (`cd5be20`) + `fix/tentative-dates` (`e96893b`) → `main`. Début de la **Phase 1 (contrat de confiance)** ; 2 lots contenus livrés (cartographie par 2 agents Explore avant édition).

**Lot 1 — `events.hidden` partout** : un event masqué depuis l'admin fuyait encore vers **13 lectures publiques** (push, digest, iCal, sitemap, page MV directe/JSON-LD, Top Rated, 4 compteurs, 2 images OG). Aucun helper commun n'existait → `.eq('hidden', false)` inline sur chaque call-site (+ `.eq('events.hidden', false)` sur le join top-rated, même pattern embedded que le `.in('events.type', …)` voisin). RLS écarté (basculer `events` en `hidden=false` casserait la vue admin — allowlist applicative sans rôle DB). **Garde anti-régression** `tests/contracts/events-hidden.test.ts` : scanne chaque `.from('events')` de `src/` et échoue si une lecture non-exemptée oublie `hidden` — la classe de bug elle-même. Admin + écritures exemptés explicitement.

**Lot 2 — dates `tentative` honnêtes** : un `tentative` sans heure est stocké à **minuit KST** (heure technique) ; l'app affichait « 00:00 KST » + countdown seconde (**20 events réels** concernés, ex. albums annoncés sans heure). Prédicat `isTimeTBA` (`status='tentative'` **ET** minuit KST — exclut les slots music-show à l'heure réelle) → « Time TBA » + suppression du countdown sur `next-drop-card`/`queue-row`/`event-card`/`landing` ; **D-day conservé** (le jour est connu, honnête).

**Vérification** : typecheck + lint + prettier + **604 tests** (dont garde hidden par-occurrence + 4 `isTimeTBA`) + `next build` verts ; 20 events tentative-minuit confirmés en prod (le fix s'applique au réel) ; CI verte par merge. **Reste Phase 1** : Lot 3 timezone bout-en-bout (décisions : anonyme→cookie, ticker), Lot 4 budget notifs, Lot 5 monitoring.

## 2026-07-15 — Phase 0 (suite) : dérive doc résorbée, sémantique dates & tiers, domaine canonique

**Branche/commit** : `docs/phase0-alignment` (`e6cd54b`) + `fix/canonical-domain` (`afd4c96`) → `main`. **Quoi** : le reste de la Phase 0 après le contrat produit. ① **Dérive doc** (`SCRAPING.md` : « Crons Vercel » → **GitHub Actions** avec les vrais schedules, music-shows 2×/j ; About : « Suggest button » → « Feedback link » du footer). ② **Sémantique des dates** définie dans `PROJECT.md` (3 états : date+heure exactes / heure TBA / `tentative`=minuit KST technique). ③ **Niveaux de confiance** définis (Vérifié / Surveillé / Candidat) — cadre la Phase 3. ④ **Domaine canonique unifié** : `resend.ts` pointait ses liens email vers `https://kstage.app` (non servi, liens cassés) alors que tout le reste est sur `https://kstage.vercel.app` ; cause racine = `SITE_URL` dupliqué en 4 fichiers → centralisé dans `src/lib/site.ts`, importé partout. **Pourquoi** : finir l'alignement promesse↔réalité de la Phase 0. **Vérification** : `source_url` (SCRAPING) et README « Live: » laissés tels quels (corrects, **faux positifs de l'audit** écartés après lecture du code) ; typecheck + eslint + prettier + tests iCal + contract-test verts ; CI verte par merge. **Décision Rudy** : rester sur `kstage.vercel.app` (`kstage.app` réservé à un éventuel domaine custom en Phase 5).

## 2026-07-15 — Contrat produit public honnête (lives retirés des claims, hero/CTA)

**Branche/commit** : `codex/public-product-contract` → `main` (merge `516f93e` ; commits `df13d0f`/`feca5dd`/`34f50d6`/`52b5e00`). **Quoi** : premier contrat produit bêta honnête, 4 tâches TDD (plan `docs/superpowers/plans/2026-07-15-public-product-contract.md`, exécuté d'abord par un agent codex, fini et livré ici). ① Hero CTA = vrai contrôle Follow/Following (`notify-cta.tsx` : `aria-pressed` + optimistic + toast générique) — fin du « Notify is on » trompeur (suivre un groupe n'implique jamais permission/abonnement push). ② Hero réservé aux vrais comebacks via `findHeroEventIndex` (`src/lib/events/hero.ts` : `mv`/`release`/`music_show`). ③ Prefs notif = 4 catégories visibles (`mv`/`release`/`music_show`/`anniversary`), ligne Lives retirée de l'UI. ④ Copie publique + docs alignées (README, layout, manifest, About, email, KSTAGE_BRIEF, PROJECT) : plus aucune promesse « live ». **Pourquoi** : cœur de la **Phase 0** (vérité produit) de la roadmap 0→5 — chaque promesse publique doit correspondre à un comportement réel avant la bêta. **Vérification** : compat interne `live` conservée partout (enum, migrations, `labels.ts`, `href.ts`, `youtube.ts`, historique) ; test de contrat `tests/contracts/public-copy.test.ts` garde contre la réintroduction ; typecheck + lint + prettier + **589 tests** + `next build` verts ; audits `rg` propres ; **code review APPROVE** (aucun bloquant ; 1 note LOW : toast possible pendant un redirect de session expirée = compromis assumé). **Décisions** : `live` reste un type interne pour l'historique, jamais une catégorie de lancement ; follow et notifications = deux capacités séparées.

## 2026-07-14 (Prêt-à-lancer) — Groupes vides remplis, premium neutralisé, audits sécu/robustesse/E2E

**Rudy : « quel serait le programme pour avoir une appli propre et prête à lancer ? ». Plan en 5 vagues (approuvé, arbitrages : paquet propre complet + neutraliser le premium). Résultat : 2 vagues de CODE livrées (data + premium), 3 vagues d'INVESTIGATION concluant que l'app est déjà en bon état — 2 merges.**

1. **Data — groupes sans membre (le seul vrai trou bloquant)** (`data/fill-empty-groups`, commit eb6688f) : 8 groupes affichaient une page artiste **vide**. Recherche vérifiée adversarialement (workflow **16 agents** : MusicBrainz + ja/ko.wikipedia + fandom + kprofiles, reconstruction double-source par entité, ancres de désambiguïsation pour écarter les homonymes). **45 membres seedés** (`scripts/roster/fill-empty-groups.ts`, data figée = reproductible, idempotent) → **0 groupe vide**. Découverte : **4 groupes mal classés `is_solo=true`** — ADYA (girl group 5), ARrC (boy group 7, **dissous 2026-06-23**), KISEO (girl group 4, re-debut d'IHOTEU), SUCTION (co-ed 4) — corrigés en groupe. `&TEAM`(9)/KiiiKiii(5)/MEOVV(5)/NCT WISH(6) complétés. Birthdays → anniversaires auto (vérifié : Ryo D-20 sur la page NCT WISH). **Photos** : `refresh-images --stale` → **45/45 nouveaux membres photographiés** (couverture **855/864 = 98,96 %** ; 9 résiduels = classe connue sans pageimage fandom, BADVILLAIN/NOWZ/OURBIRTHDAY → éditeur admin). **Alias NOWZ↔NOWADAYS** dans `group-match.ts` (+1 test, 20 verts) : le groupe est stocké « NOWZ » mais ses MVs titrés « NOWADAYS » → sans alias les FUTURS MVs ne s'auto-attribuaient plus (dette d'ingestion R11 soldée ; les 4 MVs existants étaient déjà liés par group_id, pas de backfill).

2. **Premium neutralisé** (`chore/neutralize-premium`, commit cd88adc) : le tier `premium` était **inerte** (aucun Stripe, inatteignable) mais **visible** via le cap « 10 groupes suivis » présenté comme perk. Cap rendu **inconditionnel** (`SIDEBAR_MAX_FOLLOWS`, bonne UX pour tous), prop `tier` retirée de `SidebarLeft`/`PageRails` (→ booléen `signedIn` : le gate `tier != null` servait de **signal auth**, préservé), calcul/fetch `tier` mort supprimé des 6 pages (orphelins `createClient`/`user` nettoyés dans calendar+mvs). Socle DB `profiles.tier` **conservé** pour un futur round Stripe. **−53 lignes** de code mort. tsc+eslint+prettier+**next build** verts.

3. **Sécu DB — audit, aucun changement** : les advisors WARN sont **déjà triés/acceptés** (BACKLOG 2026-06-16). Vérifié par lecture des définitions : `group_follow_counts`/`profile_stats`/`consume_rate_limit` sont `SECURITY DEFINER` **par nécessité** (agrègent des **compteurs** à travers des tables sous RLS — `user_follows`/`event_ratings`/`mv_like`/`comments`/`rate_limit_hits` — sans renvoyer aucune ligne privée) et `anon`/`authenticated`-exécutables **par design** (pages publiques + rate-limit) ; `SECURITY INVOKER` casserait l'agrégation (RLS filtrerait aux lignes du caller), révoquer casserait les pages/le rate-limit. `citext` porte `profiles.username`. **Correction d'une croyance du BACKLOG** : `ALTER EXTENSION citext SET SCHEMA extensions` NE casse PAS la colonne (déplacement par OID). **Appliqué le 2026-07-15** (migration 0052, autorisé par Rudy) → advisor `extension_in_public` **soldé** ; vérifié en prod : `username` toujours citext + case-insensitive (`'REMILIO'`='remilio'='Remilio'). Restent seulement les advisors intentionnels (2 INFO RLS deny-all + 3 fonctions SECURITY DEFINER).

4. **Robustesse UX — audit, aucun changement** : l'audit surface suggérait `loading.tsx`/`error.tsx` sur les pages détail. Investigation : `loading.tsx` sur un segment dynamique **régresserait le fix soft-404** (audit 2026-07-10 — un boundary de loading force le streaming d'un shell HTTP 200 AVANT `notFound()` ; documenté dans `page-skeleton.tsx`) ; `error.tsx` par route serait **redondant** (le boundary global `app/error.tsx` garde déjà le `<header>` du root layout et scope l'erreur au contenu). Pages détail déjà robustes (empty states, `notFound`, boundary global). **Ne rien changer = la décision correcte** (Simplicity First).

5. **E2E — specs déjà correctes** : les 4 specs (`auth`/`feedback`/`search`/`smoke`) utilisent déjà `exact:true` sur les toggles Follow/Unfollow (piège `getByRole`=sous-chaîne documenté dans le code) + garde si creds absentes. **Rien à corriger.** Blocage = **actions Rudy** : compte test dédié (les creds actuelles = compte perso, risque de mutation) + `E2E_ENABLED=true` (**Variable**, pas Secret) + 4 secrets GitHub.

**Vérification** : tsc + eslint + prettier + next build verts ; 20 tests group-match verts ; **prod SQL** (0 groupe vide, 855/864 photos, is*solo/disbanded corrigés, 0 doublon) ; **navigateur** (NCT WISH/ADYA rendent membres + positions + anniversaires ; aespa/Karina OK) ; **CI verte par merge** (a40f7f7 premium, 682054d data). **Note dev** : Turbopack a servi une erreur de parse fantôme (cache SW PWA) contredite par `next build` vert — se fier à la build, pas au cache dev. **Actions Rudy** (2026-07-15) : ① **E2E — ACTIVÉ + VERT** : les variables E2E étaient scopées à l'environnement `Production` (invisibles dans le `if:` d'un job) + le ci.yml lisait `secrets.*`là où c'étaient des`vars`→ double fix (montée au niveau repo par Rudy +`fix/ci-e2e-vars`:`vars.NEXT*PUBLIC*\*`/`E2E_AUTH_EMAIL`, `secrets.E2E_AUTH_PASSWORD`). Run `78266ee`: job`e2e` **success** (smoke + search + feedback + golden path auth contre le vrai backend). Reste conseillé : compte test dédié (`E2E_AUTH_EMAIL`= compte perso ; le test ajoute un follow, bénin depuis`exact:true`). ② Sitemap→Search Console (plus tard). ③ VAPID Prod = confirmé fait. ④ soft launch (plus tard). ⑤ citext = fait (migration 0052).

## 2026-07-14 (R11) — Récolte de données post-activation (photos, MVs, membres)

**Le socle activé (R10.1), Rudy : « récupère les images de membres, leurs MV, les lineups quand c'est possible ». Récolte des trous que les crons ne ferment pas seuls, concentrée sur les 14 nouveaux groupes.** Pas de merge de code (hors `scripts/youtube-channels.json`) — l'essentiel est de la donnée en prod.

- **Constat (scrape_log)** : les crons GH tournent déjà depuis le secret — `music_shows` 14:50/14:56 (32 events, carrd primaire OK, 6/6 lineups), `youtube_backfill` 14:43 (14 MVs). Donc **music shows = déjà auto** (rien à relancer ; les unmatched ASCENDER/IDID/UDTT = rookies absents de la DB). Le `refresh_images` de 04:05 tournait AVANT la création des groupes → leurs membres restaient sans photo.
- **Photos membres** : `refresh-images-once --stale` (phase photos seule, `photo_source_key null`) → **88 photos résolues** (les ~66 nouveaux + résidus solistes), 3 misses. 2ᵉ passe après insertions membres : +5.
- **MVs des groupes label-channel** : cause = le cron backfille la chaîne _propre_ du groupe, mais leurs clips vivent sur la chaîne du **label** (classe R7-2, `discover-mv-channels`). Découverte → seed umbrella (`youtube-channels.json` + `seed-youtube-sources`, pattern HYBE-×12, attribution par **filtre titre** `matchesGroup` — pas de flag `is_shared`) → backfill pleine profondeur (`pageCache` partage JYP entre NEXZ+KickFlip → 2 units). **65 MVs insérés** sur 8 groupes : NEXZ 13 (JYP), YOUNITE 12 (BRANDNEW), n.SSign 11, POW 9 (GRID), KickFlip 8 (JYP), BADVILLAIN 5 (BPM), Nowadays 4 (NOWZ), Candy Shop 3 (Brave). Les chaînes propres confirment 0 MV (clips bien sur le label).
- **Bannières / images** : `refresh-images-once --photos=0` → **18 bannières YT 2560px** rafraîchies (nouveaux groupes à source YT). Spotify by-ID : 0 update (nouveaux groupes sans lien Spotify, gardent l'image fandom).
- **Membres manquants** : 3 groupes à 0 membre — parse infobox échoué pour raisons **structurelles** (BADVILLAIN : champ `current=` vide, membres en table ; ARrC : que des `former`, groupe **dissous** 2024–2026 ; Nowadays : page réelle = « NOWZ », pas « Nowadays »). Extraction fiabilisée : **classification person-vs-song par infobox** (le suffixe `(GROUPE)` capte aussi les pages de titres — Thriller/Hurricane/Ignition écartés). **12 membres insérés** (BADVILLAIN 7, NOWZ 5) via la forme `createFromPayload`, idempotent par (group_id, stage_name). 5 photos résolues ; 7 misses = classe connue « page fandom sans pageimage » (→ éditeur admin). ARrC laissé tel quel (dissous, pas de date précise à inventer).

**Vérification (prod SQL)** : 11 des 14 nouveaux groupes ont roster **complet + photos** (AHOF 9/9, NEXZ 7/7, UNIS 7/7, YOUNITE 7/7, KickFlip 6/6, CORTIS/PLAVE/RESCENE/POW 5/5, Candy Shop 4/4, n.SSign 7/7) ; les 14 ont des MVs sauf ARrC (dissous). **Résiduels documentés** : 7 membres BADVILLAIN/NOWZ sans pageimage (→ éditeur admin) ; ARrC dissous. **Sécurité** : aucun cron d'envoi (`send-digest`/`notify-comebacks`) déclenché, aucun E2E activé, `seed-roster` non touché.

**Suivi — renommage « Nowadays » → « NOWZ »** (demande Rudy) : effectué (name+slug+slugs membres). ⚠️ **Découverte en route** : la raison invoquée (débloquer les photos membres) était **moot** — les 3 pages fandom NOWZ n'ont pas de pageimage (même classe que BADVILLAIN, `refresh --stale` = 0 update après rename). Et le renommage a un **coût** : `matchesGroup` exige le nom du groupe dans le titre du MV ; or les MVs sont titrés « NOWADAYS(나우어데이즈) » → `matchesGroup("NOWADAYS…","NOWZ")=false` → **les FUTURS MVs sur @cube_nowz ne s'auto-attribueront plus** (les 4 en base sont saufs, liés par group_id). Gardé NOWZ (nom fandom-canonique + instruction explicite), caveat tracé au BACKLOG (alias `matchesGroup` NOWADAYS↔NOWZ, ou backfill manuel à chaque sortie). Leçon : **nom du groupe et matching MV sont couplés** — renommer un groupe dont les MVs portent un autre libellé casse l'ingestion future.

## 2026-07-14 (R10) — Socle scraping (groupes + crons) + pages membres + rails

**4 retours Rudy, priorité au socle d'automatisation — 5 merges** (`feat/r10-gh-crons`, `feat/r10-groups`, `feat/r10-member-pages`, `feat/r10-rails`) :

1. **Crons → GitHub Actions** (débloque le socle) : `vercel.json` avait **6 crons** ; Vercel Hobby plafonne leur nombre → 3 ne tournaient plus (music-shows/digest/notify silencieux depuis le 12/07, **0 music show futur**). `.github/workflows/crons.yml` (7 schedules UTC dont music-shows 2×/jour + `workflow_dispatch`) `curl` chaque endpoint avec `Authorization: Bearer CRON_SECRET`. `vercel.json` vidé (source unique, évite le double-run YT). Un run raté/sauté est désormais VISIBLE. **⚠️ Action Rudy** : ajouter le secret repo `CRON_SECRET`.
2. **Groupes manquants (RESCENE…)** : cause racine — la découverte auto était **forward-only** (ne scannait que `Category:{annéeCourante}_debuts`) → RESCENE (2024)/PLAVE (2023)/UNIS·NEXZ jamais atteints ; et le gate laissait passer tout nugu d'une major (aucun signal de popularité → biais 2026). Fix `ingest.ts` : **backfill 2023-2025** (scan 796 pages vs ~100) + gate = date concrète ET **audience réelle** (subs YT ≥10k OU fans Deezer ≥5k, helper `deezerFans`). + `ingestNamedGroups()`/`scripts/roster/add-named-groups.ts` (ajout ciblé par nom → dossier complet `createFromPayload`). Prod : **14 groupes créés** avec dossier complet (RESCENE/PLAVE/UNIS/NEXZ + BADVILLAIN/KickFlip/CORTIS/Candy Shop/POW/Nowadays/YOUNITE/n.SSign/ARrC/AHOF), MVs au prochain scrape. 2 agences corrompues nettoyées (KISEO, QQQ — `field()` inline).
3. **Pages membres vides + hors recherche** (reco : enrichir + indexer, pas revert) : **9 membres** avaient des MVs solo (`mv_kind='member'`) **stockés mais jamais affichés** → `getMemberMvs` + section « Solo releases ». Fullness : section « groupmates ». **Recherche** : `searchMembers()` + segment/panneau « Artists » sur `/search` + membres dans le dropdown header. « Karina », « Hanni »… enfin trouvables (le sitemap indexait déjà les 677 pages → incohérence interne). Vérifié navigateur (Karina cherchable + « aespa members » ; Heejin « Solo releases » + « ARTMS members »).
4. **Rails latéraux** : `<PageRails>` (shell 3 colonnes réutilisable) appliqué aux pages groupe + membre — `SidebarLeft` (My groups, si connecté) + contenu + `SidebarRight` (recent comebacks + discussions). **Desktop-only** (`hidden lg:block`) → mobile garde la page seule, **hero full-bleed préservé** (vérifié : desktop rail à droite, mobile 0→375 edge-to-edge).

**Vérifications** : tsc + 568 tests verts par lot ; CI verte ; **prod SQL** (14 groupes dossiers complets, agences nettoyées) ; **navigateur** (recherche membres, pages enrichies, rails desktop/mobile). **Différé** : parsing carrd music-show (numéros d'épisode Music Bank/Inkigayo/The Show — moot tant que le cron GH ne tourne pas → backlog) ; rails sur le MV individuel (player + comments realtime) ; me:I (titre fandom différent). **Action Rudy** : secret `CRON_SECRET` (sinon les crons GH renvoient 401).

## 2026-07-14 (R9) — Reste-à-faire : photos solistes, In memoriam, éditeurs admin

**État des lieux du reste-à-faire (3 explorations) + 2 décisions Rudy (In memoriam OUI, Stripe reporté) — 3 merges** (`fix/r9-soloist-photos`, `feat/r9-in-memoriam`, `feat/r9-admin`) :

1. **Photos des 12 solistes datés** (suite R8.1) : le nom nu tombait sur une désambiguïsation OU redirigeait vers un homonyme (Jisoo→Jisu, Soojin→Sujin). Ajout de candidats **désambiguïsés DIRECTS** (parent via la row membre en groupe non-solo : `Jisoo (BLACKPINK)`, `Taemin (SHINee)`) tentés AVANT le nom nu, + `(singer)` en title-case mot-à-mot (`Yena (singer)` alors que la DB stocke « YENA » — classe casse), + `SOLO_PARENT_FALLBACK` pour les solistes dont le groupe a quitté le roster (Taeyeon/SNSD). **12 → 1** (reste Paul Kim, aucun pageimage fandom → manuel). A.C.E + Highlight : pas de `{{Instagram}}` en infobox → saisie manuelle (éditeur admin). 2 imprécisions BACKLOG corrigées (badge tier non rendu ; trigger autorise déjà le service_role).
2. **In memoriam** (décision Rudy) : Jonghyun (SHINee) et Moonbin (ASTRO) étaient **absents** de la base. Migration 0049 (`member_status += 'deceased'`, dédiée — ADD VALUE inutilisable dans sa txn) + 0050 (insertion), photos sourcées par le pipeline fandom. Rendu **« In memoriam », jamais « Former »** : STATUS_LABEL, member-card (label + **pas grisé**), `getCareerPath`, et **section « In memoriam » DÉDIÉE** sur la page groupe (entre Members et Former ; le compteur « Members » reste sur les actifs). Vérifié navigateur (SHINee : section In memoriam, Jonghyun opacity 1, Members — 4 intact).
3. **Éditeurs admin ciblés** (décision Rudy : Studio + in-app) : `requireAdmin()` extrait (était dupliqué), `selfHostImage()` partagé. **Hub `/admin`** (grille gatée ; le bloc profil pointe dessus). **`/admin/images`** : éditeur photo membre par URL (aperçu + self-host ; `photo_source_key='admin'` → le cron ne l'écrase plus, garde ajoutée). **`/admin/events`** : corriger un titre MV (`updateEventTitle`) + **masquer un faux event** (`setEventHidden`) — migration 0051 `events.hidden` + filtre `.eq('hidden', false)` sur les requêtes display (calendrier, upcoming, grilles MV, comebacks, trending) ET recherche (« masqué » ≠ « cancelled »). Effet levier : Rudy fait désormais lui-même les saisies manuelles (IG A.C.E/Highlight, Weverse).

**Vérifications** : tsc + 568 tests verts par lot ; CI verte (Lots 1-2 `success`, Lot 3 en cours) ; **prod SQL** (12→1 stale, Jonghyun/Moonbin `deceased` + photo) ; **navigateur** (In memoriam SHINee, `/admin` gaté → login, filtre hidden non-breaking aespa 32 MVs). **Différé** (dépendances externes) : Stripe (compte Rudy + perks), KARD `disbanded_on` (28/07), The Show ep 394 (board SBS muet), ères BEAST→Highlight + solos membres (feature scraper ~20-30 MVs), ops Rudy (secrets E2E, Search Console).

## 2026-07-14 (R8.1) — Reprises : photos (racine + audit), réseaux Lisa (source + scan), modals, recherche

**5 retours Rudy — dont 3 récidives de classe (« corrige à la source, scanne toute l'app »). 4 merges** (`fix/r81-member-photos`, `fix/r81-soloist-socials`, `perf/r81-modals`, `perf/r81-search`) :

1. **Photos datées (Kim Chaewon, ARTMS) — corrigé à la RACINE + audit de classe.** L'hypothèse « tiret » était fausse (réfutée live). Vraies causes dans `refreshMemberPhotos` : (a) qualificateur construit depuis la casse DB `(Le Sserafim)` alors que le wiki exige `(LE SSERAFIM)` (MediaWiki sensible à la casse après la 1re lettre) ; (b) `titleCase` détruit le camelCase `HeeJin`/`HaSeul` (LOONA/ARTMS) ; (c) repli real_name neutralisé si `real_name==stage_name`. **Faille de classe** : un miss conservait la photo stale → split d'ère permanent. Fix : variante upper du qualificateur + **repli `generator=search`** (garde de catégorie conservée) + option `staleOnly` + `scripts/audit-member-photos.ts` (groupes à sourcing mixte). Passe `--stale` : **153 re-sourcés, 14 miss. 109 stale → 12, 27 groupes mixtes → 0.** LE SSERAFIM 5/5 même ère (Iconic By Mistake, Kim Chaewon incluse), ARTMS 5/5. Restent 12 **solistes** (pages désambiguïsées, homonymes — le repli les exclut pour ne pas rejouer Lisa/LiSA).
2. **Réseaux de Lisa = LiSA japonaise — corrigé + source blindée + classe scannée.** `seed-artist-links` cherchait MusicBrainz avec `limit=5` : pour « Lisa » le top-5 = LiSA JP (score 100), la vraie Lisa BLACKPINK (KR, 83) rang ~17 → bloc social entier = la japonaise. Migration 0048 (valeurs BLACKPINK vérifiées, `lalalalisa_m`…). Source : `limit 5→25` + **garde `country=KR`** (plus de fallback homonyme non-KR). `scripts/scan-soloist-socials.ts` sur les 33 solistes (infobox fandom désambiguïsée via la row membre du parent) : **8 « mismatches » mais SEULE Lisa est un vrai faux** ; les 7 autres (iu `dlwlrma`, jennie `jennierubyjane`, sunmi `miyayeah`, taemin `taeminbpm`…) = comptes perso officiels stockés vs comptes fan/agence/anciens sur fandom → **NON touchés** (les écraser aurait introduit des erreurs — vérifié un par un).
3. **Modals lentes (feedback, bias, avatar) — cause partagée.** `dialog.tsx` empilait un `backdrop-blur-sm` plein écran par-dessus 2 autres `backdrop-filter` (header + bottom-bar) + démontage différé Base UI. Fix : blur retiré (voile opaque conservé) + `duration-150`. Bias picker : `getAllMembers` (675 rows) en `unstable_cache` 1h. Avatar cropper : `react-easy-crop` en import dynamique + modale ouverte immédiatement (spinner) au lieu d'attendre le downscale canvas. **Vérifié navigateur** : `backdrop-filter: none`, animation 0,15 s.
4. **Recherche « aespa » 3-4 s** — la DB répond en 42 ms ; les 3-4 s = cold start du lambda Node. Segment **groupes 100 % client** (`allGroupsForClient` cachée + `/api/search/groups`, ~150 groupes servis une fois, filtrés localement) → « aespa » instantané. `/api/search/quick` réduit aux MVs (`withRatings:false`, plus de round-trip ratings). **Bug attrapé en vérif navigateur** (invisible à tsc + 568 tests) : `getGroupSubscriberCounts` lit les cookies → interdit dans `unstable_cache` → refait en client anon.

**Vérifications** : tsc + 568 tests verts par lot ; CI verte (Lots 1-3 `success`, Lot 4 en cours) ; **prod SQL** (LE SSERAFIM/ARTMS 5/5 fandom, Lisa `lalalalisa_m`) ; **navigateur** (recherche aespa client+serveur, modal sans blur/anim 150 ms, 0 erreur console). Leçon (re)confirmée : la vérif navigateur attrape ce que tsc+tests ne voient pas (crash cookies-in-cache) ; et une seule source (fandom) n'est PAS vérité terrain (comptes fan/agence).

## 2026-07-14 (R8) — Round 8 : bugs data (Lisa/ARTMS/anniversaires), réseaux membres, perf mobile, zoom input, logo

**11 retours Rudy + 3 décisions produit — 5 merges (`feat/r8-data-fixes`, `feat/r8-socials`, `feat/r8-mobile-pwa`, `chore favicon`, `perf/r8-mobile`)** :

1. **Lisa = LiSA japonaise (bug de lien seedé, pas de fetch)** : `groups.links.spotify` du slug `lisa` pointait `0blbVefuxOGltDBa00dspv` = **LiSA** (chanteuse Demon Slayer). La garde de nom `spotifyNameMatches` NE PEUT PAS l'attraper (les deux se normalisent en « lisa »). Corrigé vers le vrai ID BLACKPINK (`5L1lO4eRHmJ7a0Q6csE5cT`), image re-fetchée. Balayage phase-1 des homonymes = 0 autre mismatch.
2. **Faux MVs ARTMS (classe vlog)** : 3 rows `type='mv'` étaient des vlogs (« MV 속 Club Icarus… EN JP CN ») passés car « MV » dans le titre. `is-official-mv.ts` durci de **7 règles blacklist ciblées** (content-tag « EN JP CN », « MV shoot », « Let's Play », « MV time », vlog maquillage 메이크업, « funniest », jalons de vues 돌파/달성/공약). **Pas de règle emoji** (elle cassait ROSÉ « F1® » et TXT « LO$ER=LO♡ER » — ® et ♡ sont Extended_Pictographic). Balayage prod : **21 faux MVs purgés** (ARTMS ×3 + tripleS, MCND Let's Play, TRI.BE jalons…), 0 faux positif. 8 titres réels ajoutés en tests de régression.
3. **Anniversaires en double (soliste = membre)** : Hwasa/MAMAMOO, Lisa, Solar… ont **2 rows membre** (groupe + solo `is_solo`) même date → 2 anniversaires. `dedupePersons()` dans `anniversaries.ts` déduplique par `normalize(stage_name)|MM-DD`, garde la row du groupe non-solo. Vérifié : Hwasa → 1 anniversaire. `is_solo` ajouté aux selects (+ route iCal).
4. **Réseaux mal affichés (ZB1 « sans Instagram »)** : ZB1 **avait** son IG en base — c'est l'affichage : `StatsStrip` → `<LinksBar compact>` plafonnait à 6 icônes streaming-first → réseaux coupés. Ajout `COMPACT_PRIORITY` (spotify, IG, YouTube, Weverse, TikTok, Twitter…), cap 6→8. **18 groupes** manquaient vraiment l'IG → backfill fandom en a récupéré 9.
5. **Réseaux des membres (feature)** : migration `members.links jsonb` (0047), rendu via `LinksBar compact` sur la page artiste, **member cards re-navigables** (`<Link>`), Weverse ajouté aux ENTRIES (icône SVG custom, `SiWeverse` absent du pack). Backfill Instagram par page membre fandom : **314 membres** dotés. Weverse = manuel plus tard (décision : aucune source structurée, 0/150 groupes).
6. **Photos LE SSERAFIM** : candidats de titre `real_name` ajoutés à `refresh.ts` (Sakura → « Miyawaki Sakura »). Sakura résolue ; Kim Chaewon reste (page « Kim Chae-won » à tiret) → photo actuelle conservée.
7. **Zoom recherche mobile (iOS)** : inputs < 16px zooment au focus. Champs recherche (14/12px) + auth/account + feedback → `text-base md:text-sm` (16px mobile). **Pas** de `maximum-scale` (a11y WCAG 1.4.4).
8. **Logo incohérent desktop/mobile** : 3 artworks « K » divergents. `generate-icons.mjs` alignée sur `icon.svg` (gradient periwinkle→teal = `--primary`), PNG PWA/apple/badge régénérés, `favicon.ico` périmé (22 mai) retiré → Next retombe sur `icon.svg`.
9. **Perf mobile** : le changement de mois refetchait les ~150 groupes NON cachés (nav RSC) → `getGroupsCached`. La recherche tapait Supabase 3× par frappe pour la liste groupes → `unstable_cache` (anon, 1h, tag `groups`). Hero home : `unoptimized` retiré (i.ytimg.com whitelisté) → webp redimensionné + `priority` (LCP). Nav month full-client reportée (backlog, gain marginal une fois les groupes cachés).

**Décisions produit (AskUserQuestion)** : ① **Monétisation** = abonnement **2,99€ à avantages, zéro pub** (Stripe, round dédié). ② **Admin** = **Supabase Studio** (CRUD brut, dispo maintenant) + éditeurs in-app ciblés (round dédié — surface privilégiée à ne pas bâcler). ③ **Weverse membres** = manuel plus tard.

**Vérifications** : 568 tests + tsc verts à chaque lot ; CI verte (A/B+C/E+F/favicon `success`) ; **prod SQL** : Lisa bon ID + image fraîche, ZB1 IG visible, Hwasa 2 rows (dédup query-time), 314 membres avec réseaux, ARTMS vlogs purgés. **Reportés (round dédié)** : Lot G éditeurs admin in-app, Lot H abonnement Stripe.

## 2026-07-13 (soir, R7) — Round 7 : MVs sur chaîne d'agence (VAYONN), carte MV revert, fil commentaires, fix trending

**3 retours Rudy + 1 bug latent trouvé — 3 commits mergés (`feat/r7-polish`) + data** :

1. **MVs sur la chaîne de l'AGENCE (classe systémique)** : VAYONN avait 0 MV car ses clips sont sur la chaîne iNKODE, pas sa chaîne perso (seedée au debut). Découverte (discover-mv-channels.ts sur 33 groupes fins) → schéma confirmé : les groupes debut sont créés avec leur seule chaîne perso alors que les labels hébergent les MVs. **7 chaînes agence seedées** (title-match = 0 sur-attribution, comme HYBE/SMTOWN) : VAYONN+KEYVITUP→iNKODE (UCTItOHr70Tq_GAOACSGawPw), ChaDongHyeop→woolliment, dodree→JYP, LNGSHOT→MORE VISION, PJX→Charon, UNCHILD→High Up. **+18 MVs** (VAYONN 0→2, KEYVITUP 0→3, LNGSHOT 0→8…), total **2 297, 0 sur-attribution vérifiée**. Cas particuliers documentés (BACKLOG) : AEN pré-debut (0 MV normal), CHASER (pas de chaîne fiable, nom trop générique), SUCTION (1theK-only, playlist > cap API 20k), MiiWAN (idole virtuelle).
2. **Carte MV drops** : le format « JUN 14 '26 » du R6 rejeté ET il tronquait toujours le nom de groupe. Retour au **mois-année** (« Jun 2026 », KST, sans jour ni apostrophe) sur une ligne qui **passe à la ligne** au lieu de tronquer (line-clamp-2, plus de truncate) — vérifié browser : « DAILY:DIRECTION · Jul 2026 » garde nom + date entiers. Même format Top Rated.
3. **Fil commentaires (3e itération, capture Reddit)** : le `[−]` à côté de chaque avatar retiré — le **trait vertical cliquable** (aligné sous l'avatar parent, x=10px vérifié) gère le repli. **Halo de fond au survol supprimé** (zone cliquable 16px transparente, le fil s'éclaire subtilement via group-hover). Replié : chevron discret au lieu de « [+] ». Vérifié browser (clic → repli OK, alignement avatar/trait = 10px des deux côtés).
4. **Bug latent trouvé** (le build R7 l'a révélé via un test flaky) : `trendingReason` ne passait pas le `now` reçu à `formatDDay` → le label D-day retombait sur l'heure réelle et affichait « D+1 » sur un event FUTUR quand l'horloge serveur passait minuit KST. now threadé.

**Vérifications** : 556 tests + build verts ; browser (carte MV wrap, fil commentaires repli + alignement) ; SQL (0 sur-attribution, VAYONN=2, LNGSHOT=8). **Prévention (BACKLOG)** : la découverte de chaîne agence devrait être périodique pour les catalogues fins/debuts — pour l'instant fait manuellement quand signalé.

## 2026-07-13 (soir) — Round 6 : photos cohérentes par ère, 168 titres réparés, dates nettes, clustering calendrier

**6 retours Rudy — 4 commits mergés (`feat/r6-polish`)** :

1. **Photos membres — 3 causes empilées derrière « Ningning en retard »** : ① la réponse MediaWiki est PAGINÉE dès que categories/pageimages débordent (sans suivre `continue` + `pilimit=max`, des pages arrivent SANS image — Ningning tombait après la coupure) ; ② le qualificatif « (groupe) » n'existe sur fandom qu'en cas de collision de noms → candidat nom NU (accepté seulement si les catégories de la page confirment le groupe — une rookie sans page ne peut pas hériter d'une homonyme) ; ③ romanisations divergentes (« Ahn Yujin » vs page « An Yujin ») → candidat « dernier mot (groupe) » via les redirections. **+259 photos d'ère courante au total (523/620 sourcées fandom)** ; aespa homogène (Kiss N Tell ×4), IVE homogène (Lucid Dream ×6).
2. **Titres — balayage de classe** : le « - Friday Night » signalé n'était que le sommet — script de balayage sur les **2 279 titres MV prod → 168 mal rendus**, 6 classes corrigées (tiret orphelin EPEX, quotes mixtes ‘…' NAVILLERA, doubles courbes “…” JYP/ITZY, ’ ouvrante THE BOYZ, tags [MV]/(MV) + coréen d'abord + `_` sans espaces Starship/WM, chanson entre crochets DSP/KARD). **Re-balayage final : 0/2 279.** 7 tests de régression. Leçon : l'outil de balayage (rejouer le renderer sur TOUTE la donnée prod) attrape en 30 s ce que des retours au compte-goutte mettraient des semaines à couvrir.
3. **Top Rated** : date de sortie (« JUN 14 '26 ») à la place du nombre de ratings. **Carte MV** : nom de groupe truncate à gauche, date complète `shrink-0` à droite — DAILY:DIRECTION ne tronque plus jamais la date (helper `shortDateYear`).
4. **Calendrier** : `clusterByGroup` — chronologie conservée mais deux events du même groupe le même jour se suivent (ancrage au premier). 2 tests.
5. **Fil commentaires** : ligne/coudes en `muted-foreground/35` (1px net dans les deux thèmes — « trop épais, trop peu visible » venait du contraste, pas de l'épaisseur), bande de repli quasi invisible au hover.

**Vérifications** : 556 tests + build verts ; SQL prod (aespa/IVE homogènes, photo_source_key) ; balayage titres 0 défaut.

## 2026-07-13 (après-midi) — Round 5 : accents Unicode, stages SBS, nommage court, UI vivante, crons Paris

**10 retours Rudy — 5 commits mergés (`feat/r5-polish`)** :

1. **Pliage d'accents** (`fix/matching`) : l'ère solo 2024 ENTIÈRE de Rosé (APT., number one girl, toxic till the end) était invisible — sa chaîne titre avec un é DÉCOMPOSÉ (e + U+0301, style macOS) que `normalize()` strippait d'un côté mais pas de l'autre. NFD + strip \p{M} + NFC des deux côtés (hangul inchangé, prouvé par test). **Re-backfill : +53 MVs (2 225 → 2 278), 0 sur-attribution.** Enquête menée en rejouant les gates sur le titre BRUT de l'API (les 3 gates passaient sur le titre retapé à la main — le bug n'était visible qu'aux codepoints).
2. **Stages Inkigayo** (`fix/stage-links`) : les 3 stages du 12/07 existaient sur @sbskpop mais SBS a changé son format de titre (« Song - Group | SBS 260712 방송 », sans « Inkigayo ») → marqueur étendu (+ The Show, même famille SBS), les 3 liés et vérifiés par oembed. Music Bank s'était auto-réparé (les stages remontent parfois à J+1/J+2 — la fenêtre 10 j du cron les rattrape).
3. **Nommage court unifié** : `displayEventTitle` délègue à `displaySongTitle` pour type=mv → Top Rated, bandeaux calendrier, hero, digest, iCal affichent le nom de la chanson seul. Ticker : « AESPA · RICH MAN D-11 » au lieu de « comeback » générique, et séquence répétée avec séparateur ◆ à chaque couture (plus de reset visible).
4. **UI vivante** : fil de commentaires v2 (tronc sous le [−] qui S'ARRÊTE au coude arrondi du dernier item, bande de repli cliquable — capture Reddit de Rudy) ; tri Top/New des commentaires et pills All/Following + new/top de /mvs passés en **100 % client** (plus de navigation ?sort=/?feed= qui re-rendait la page) ; lien Calendar au niveau du header Upcoming (groupe + artiste) ; slots « Lineup TBA » avec l'avatar de la chaîne officielle du diffuseur ; **heure locale en avant partout** (queue-row, next-drop — event-card l'était), KST en référence.
5. **Crons retunés** (référence Paris, table complète SCRAPING §Cron) : scan YouTube 03:00→10:00 UTC (19h KST, 1 h après le créneau de sortie 18h KST + après le reset de quota), digest 10:00→07:45 Paris, rappel comeback 11:00→10:30 Paris (~30 min avant les sorties). Music-shows/comebacks/images inchangés (déjà optimaux).
6. **Photos membres** : 2 sous-classes de misses corrigées — casse mi-mot (« NingNing » ≠ page « Ningning », MediaWiki sensible à la casse après la 1re lettre → variante Title-case demandée en plus) et solistes (« Jennie (Jennie) » n'existe pas → titre nu). **+93 photos d'ère courante (265/534)** ; le reste = pages hors convention, la rotation quotidienne continue.

**Vérifications** : 547 tests + build verts avant merge ; stages Inkigayo contrôlés par oembed ; APT./Rosé revérifiés en SQL ; CI #283. **Leçon** : pour un rejet silencieux de scraper, rejouer les gates sur la donnée BRUTE de l'API, jamais sur une copie retapée.

## 2026-07-13 — Round 4 : catalogues COMPLETS (+1 155 MVs), images à la source, rosters audités, debuts automatiques

**12 retours Rudy + consigne méta** (« une erreur repérée = une classe à chercher partout, corriger à la source ») — 6 investigations parallèles puis 5 merges :

1. **Catalogues MV — la vraie cause** (`feat/r4-data-core`) : PAS la profondeur de HYBE (voir correction ci-dessous) — la pagination s'arrêtait au défaut `max-pages=12` (~600 vidéos/chaîne) et le « run profond » du 12/07 n'a couvert que les 14 nouvelles chaînes (`--slugs`). Fix backfill : `--max-pages=0` (illimité), `--budget` (un 403 quota mi-run tronquait en silence), cache de playlists partagées (HYBE ×12 sources, SMTOWN ×10 : −40 % d'units), trace scrape_log `youtube_backfill`. **Run complet : +1 155 MVs (1 070 → 2 225), 3 720 units, 0 sur-attribution** — BTS 3→63, SEVENTEEN 5→60, TWICE 5→42, ITZY 3→27, LE SSERAFIM 4→21, WEi 3→16 ; 31 groupes fins → 3 restes légitimes (Soojin 3 clips réels, TRI.BE chaîne introuvable, FIFTY FIFTY catalogue court). `SCRAPING.md §3.19`.
2. **Images à la source** (`feat/r4-data-core`) : l'image de WEi était **« Weird Al » Yankovic** (search Spotify par nom → `items[0]` sans garde) et le cron hebdo re-corrompait chaque lundi ; TXT pointait un faux artiste « T.X.T. » (lien mal seedé) + une pochette fan-art. Pipeline réécrit (`src/lib/images/refresh.ts`, cron **quotidien**) : ① `image_url` ← Spotify **par ID** (`links.spotify`) + garde de nom (mismatch → log, zéro écriture) ; ② `banner_yt_url` (colonne dédiée, migration 0042) ← bannière de la chaîne YouTube **exclusive** au groupe (`brandingSettings` + `=w2560`, 2560×1440, mise à jour par les labels à chaque ère) — **100/112 groupes servis**, fini le hqdefault 480px étiré ; ③ photos membres ← **kpop.fandom API** (photos d'ère courante, clé de changement `cb=`, self-host, rotation ~100/jour) — **172 passées à l'ère courante** (Karina = concept photo du 02/07). Chaîne bannière unifiée `groupBannerSrc()` sur groupe/artiste (drops n'affiche plus de bannières). Deezer + TheAudioDB supprimés du cron (même classe d'erreur par-nom + fanarts 2018-2021).
3. **Rosters audités** (`feat/r4-data-core`, migrations 0043-0044) : la classe « Garam active » = **32 membres partis encore actifs** (20 groupes, chacun vérifié Wikipedia le 12/07 : Jae/DAY6, 4 fromis_9, 4 ZEROBASEONE mars 2026, 6 Secret Number, Seunghan/RIIZE…), doublon FTISLAND fusionné (Jonghoon/Jonghun = même personne), **`groups.disbanded_on`** (CIX 2026-04-29, Loossemble 2024-11-29) + badge héros, section Former remontée sous Members, **13 membres actuels manquants ajoutés** (fiches vérifiées multi-sources : KARD BM/J.Seph, N.Flying Dongsung, FIFTY FIFTY relaunch ×4, Secret Number lineup 2025 ×4, AKMU Chanhyuk) + Keena renommée légalement (Song Yuni). Cause racine : rosters seedés à T, jamais re-validés — 7 départs sont postérieurs au seed.
4. **UI** (`feat/r4-ui-polish` + `feat/r4-drops-desk`) : Contribute fusionné dans le widget Feedback (3e catégorie **Data**, texte libre, migration 0045 ; formulaires structurés supprimés, /admin/suggestions conservé pour drainer) ; fil de commentaires **léger** (1px couleur border + coude arrondi Reddit vers chaque réponse + `[−]`, hit area élargie) ; bloc Settings /u supprimé (ThemeToggle du header rendu visible en mobile — il était le seul accès mobile au thème) ; **Drops « MV Desk »** : hero du dernier drop (maxres + repli hq), Top rated remonté, grille avec filtre All|Following — les rails par groupe (source d'images divergente + visages coupés en bande 64px) supprimés.
5. **Music shows durcis** (`feat/r4-ui-polish`) : verdict d'enquête — les sources n'étaient PAS en panne (pas de show le lundi 13/07 ; Inkigayo 12/07 ingéré à J-1). Vrais trous corrigés : fallback broadcaster déclenché aussi sur **lineup maigre** (<3 artistes, remplacement même show/jour), **solos de membres mappés** (« KIHYUN (MONSTA X) » via parens, « YEONJUN » via stage_name actif sans homonyme → TXT), **alerte J-1** (show à <36 h sans lineup ni row → scrape_log `partial` + `stale_alerts` — le signal qui manquait quand Inkigayo est resté « matched:0 » 4 jours en juillet). ⚠️ À surveiller : le board SBS The Show (fallback) n'a plus posté depuis nov. 2025, relance « SBS LiFE THE SHOW » le 14/07.
6. **Debuts automatiques** (`feat/r4-debut-discovery`, migration 0046) : étage 3 du cron scrape-comebacks — diff quotidien de `Category:{YYYY}_debuts` (kpop.fandom API, idempotence par pageid), parse d'infobox (nom/date/label/membres/SNS/image), **gate d'auto-création** (date concrète ET notabilité : liste Wikipedia OU chaîne YT vérifiée `forHandle` ≥10k subs OU label connu), le reste en revue `/admin/debuts`. Création complète : groupe (image fandom self-hostée, bucket group-photos) + lineup + **source youtube_api vérifiée** (les crons existants font le reste : MVs, releases kpopofficial, images) + event debut ; pages pré-debut = badge + noindex (cohérent page-pruning). **Premier run : 5 groupes créés et vérifiés** (AEN — debut 05/08 Starship, ALPHA DRIVE ONE 754k subs, AmbiO, AND2BLE, B:DAWN), 7 en revue, 0 erreur ; drain du stock (133 pages) en cours.

**Vérifications** : 544 tests + build verts avant chaque merge ; CI GitHub verte (#278-280, #281 en cours) ; SQL prod (0 sur-attribution, 45 formers, 2 disbanded) ; SSR /mvs (hero + grille). **Correction d'hier** : la limite « BTS trop profond dans HYBE LABELS » du 12/07 était FAUSSE — le catalogue HYBE complet = 68 pages, entièrement paginable (vérifié par pagination réelle jusqu'à 2008). **Décisions** : membres décédés (Jonghyun, Moonbin) non modélisés pour l'instant (proposition enum `deceased` + « In memoriam » au BACKLOG) ; KARD : dissolution annoncée le 06/07 (dernier album 28/07) — `disbanded_on` à poser quand effective ; mapping des titres fandom à améliorer (362/534 photos membres non résolues au premier passage — la rotation retente).

## 2026-07-12 — Round 3 : 18 retours Rudy — bugs, calendrier client, pages revues, catalogues profonds

**11 branches/commits mergés** (plan approuvé, lots A→N) :

1. **Doublons time-shift** (`fix/music-show-timeshift`) : le carrd a révisé l'heure du Music Core 954 (15:15→15:20 KST) → 2 rows/groupe. Idempotence + réconciliation + fusion d'affichage passées au **jour KST** (`kstDayBounds`, self-heal avec compteur `updated`), purge one-shot. `SCRAPING.md §3.18`.
2. **Like MV réparé** (`fix/mv-like`) : l'action ne revalidait aucun chemin → cache périmé → ré-insert → 23505 → error boundary. `revalidatePath` + insert idempotent + toast. Reproduit et vérifié au compte e2e.
3. **Quick-wins UI** (`feat/r3-ui-quickwins`, 5 commits) : vignettes MV sans chip « Rate » ni bordure dashed ; Top Rated avec avatar de groupe et sans tiret ; cœur Trending en variante `subtle` ; profil (stats sans redondance ult/bias, ring avatar recentré 64/64, Settings fusionnés) ; recherche normalisée (« idle » → i-dle) ; « Single - Crow » → « Crow » (extraction après le dernier en-dash, 7 surfaces, tests sur variantes réelles).
4. **Pages artiste/groupe** (`feat/r3-artist-pages`) : ordre Members > Upcoming > MVs, grille MV complète 2/3/4 col (fin du rail + « See more »), Contribute au header du panneau, âge des membres à la place du « — », noms en line-clamp-2, bannières solo alignées (Jennie servait un fanart 2018).
5. **Page MV** (`feat/r3-mv-page`) : étoiles retirées, ShareButton supprimé partout, avatar groupe au header + « More from {group} ».
6. **Commentaires repliés** (`feat/r3-comments-collapsed`) : racine + meilleure réponse, le reste sur « Show N replies ».
7. **Calendrier 100 % client** (`feat/r3-calendar-client`) : CalendarFilterProvider — le mois entier chargé une fois, groupes/types filtrés en mémoire (bascule instantanée), URL réduite à `?month` (+ deep-link `?group` conservé), doublon de scope supprimé. Golden path E2E mis à jour (chips = boutons aria-pressed).
8. **Notifications** (`feat/r3-notifications`) : badge Android monochrome dédié, `tag`+`renotify` (digest remplace digest, rappels d'un comeback se remplacent), titre quotidien « Today in k-pop: N events », body en « · ».
9. **Data** (`feat/r3-data-catalogs` + exécutions) : photos solistes ← image Spotify (31, re-self-hostées) — kprofiles est lui-même figé (aespa = photos 2020 vérifiées sur la page live), donc pas de re-scrape membres de groupes ; **14 chaînes labels/officielles découvertes par recherche de MVs réels** (BIGBANG, cube_ptg, FNC, Stone Music, Official fromis_9, BRANDNEW, BLACKPINK, izna 2M…) → backfill profond **+121 MVs (949 → 1 070, 0 sur-attribution)** : FTISLAND 3→29, G-Dragon 3→15, fromis_9 2→9, Dreamcatcher 2→9.
10. **Hotfix build** (30157ad) : le script discover inférait `never[]` → `next build` rouge sur le merge R3-B (run #274), réparé en 10 min (#275 vert). Leçon : build complet avant merge d'un lot qui ajoute un script.

**Vérifications** : 544 tests unitaires + 27/28 E2E CI-mode verts ; prod re-testée (calendrier client actif, « Everyone » disparu, bannière Jennie = thumbnail dernier MV, Share absent, fromis_9 = 9 MVs). **Limites documentées** : BTS reste à 3 MVs (~~catalogue trop profond dans HYBE LABELS~~ — **corrigé le 13/07 : c'était faux**, la vraie cause était `max-pages=12` par défaut, cf. entrée 2026-07-13) ; TRI.BE introuvable par recherche ; classiques 1theK partiels. ⚠️ Le job e2e GitHub est toujours « skipped » malgré l'action de Rudy — vérifier que `E2E_ENABLED=true` est bien une **Variable** (pas un Secret).

## 2026-07-11 (après-midi) — Round 2 : commentaires Reddit, Top Rated périodes, Trending, hero frais, scrapers fiabilisés

**6 demandes Rudy, 6 branches mergées** (investigations multi-agents + diagnostics SQL prod d'abord) :

1. **`fix/music-show-accuracy`** — l'épisode M Countdown EP.936 (09/07) a révélé 3 défauts : stage-link = interview (« '컴백 인터뷰' … » scorait +3 → malus -5 sur interview/behind/making/fancam…), Hearts2Hearts manquant (typo carrd « Heart2Hearts » → fuzzy matching 1 édition sur clés ≥ 8 chars), 5 rows fantômes Music Bank 10/07 (lineup révisé jamais réconcilié → le cron réconcilie désormais les épisodes futurs). Data corrigée et **vérifiée par oembed** : i-dle → vrai stage « Crow », H2H EP.936 créé → « Lemon Tang ». Cf. `SCRAPING.md §3.16`.
2. **`fix/mv-derivative-title-only`** — le MV « Lemon Tang » (SMTOWN) était silencieusement rejeté : la piste 05 de l'album s'appelle « Secret Recipe » et `\brecipe\b` matchait la tracklist en DESCRIPTION. Gate dérivés → titre seul ; re-backfill global des 159 sources lancé. izna avait 0 MV (source seedée le 17/06 > sortie METRONOME 08/06, backfill jamais lancé) → comblé. Cf. `§3.17`.
3. **`fix/group-hero-freshness`** — la page groupe était la seule surface à servir `image_landscape` (fanart TheAudioDB 2021, jamais rafraîchi — le « aespa daté » de Rudy) : le hero prend désormais le thumbnail du dernier MV `main` (hqdefault, toujours servie) avant le landscape. 52 groupes concernés.
4. **`feat/trending-signal`** — Trending /groups : score du moment (imminence event 45 j ×3 + récence sortie 30 j ×2, follows en départage), sous-titre uniforme (« Comeback · D-3 » / « MV out · 3d ago »), lien « All » (self-link) retiré. Lib pure + 6 tests.
5. **`feat/top-rated-periods`** — Top Rated Month/Year/All-time avec la BONNE sémantique (MVs SORTIS dans la fenêtre — l'ancienne fenêtrait la date de pose des notes : périodes indiscernables). Week abandonnée (structurellement vide). Segmented control client préchargé, badge « New » < 7 j, deltas retirés (sans objet). 7 tests.
6. **`feat/comments-reddit-polish`** — permalinks par commentaire (ancre + timestamp cliquable + flash :target), rail de repli à TOUS les niveaux, auto-repli score ≤ -3, cap d'indentation depth 6. Volume prod (14 comments) → zéro changement DB.

**Constat couverture scraping** (demande « tous les artistes sont-ils cherchés ? ») : 112/112 groupes ont une source YouTube, 0 source périmée, crons quotidiens verts — l'infra est saine ; l'impression « peu de MVs » = cadence réelle (~14 MVs sortis/30 j sur le roster) AMPLIFIÉE par le bug §3.17 (MVs perdus) et les solos de membres (YEONJUN, KIHYUN, HAN… — leurs MVs ne portent pas le nom du groupe, limitation connue). **Re-backfill global post-fix : +76 MVs récupérés (871 → 949), 1 811 units, 0 sur-attribution vérifiée.**

## 2026-07-11 — Perf/ingénierie : Promise.all home, policies feedback, comeback-ingest batché

**Branche** : `fix/eng-batch` (6d48af5). **Quoi** : profile+follows de la home en `Promise.all` ; migration 0041 (policies `feedback` recréées avec `(select auth.uid())` — les 0036/0037 avaient réintroduit le pattern initplan nu) ; `ingestComebacks` dé-N+1 (2 pré-fetch batchés + décision mémoire + insert par paquets de 100 avec repli ligne-à-ligne — l'ancienne boucle faisait 1-2 requêtes séquentielles PAR candidat, ~200 entrées wikipedia/run). **Vérification** : advisors re-runés — `auth_rls_initplan` à 0, reste = baseline INFO ; 523 tests verts. Le run scraper du lendemain confirme le comportement batché (scrape_log).

## 2026-07-11 — Contenu : slots hebdo synthétiques des shows (P0.8 tranché) + 4 groupes fantômes remplis

**Branche** : `feat/synthetic-show-slots` (c507f13). **Quoi** : (1) `generateShowSlots` — les 6 music shows affichés sur calendrier + week glance AVANT publication des lineups (« Lineup TBA »), calculés à la lecture comme les anniversaires (jamais en DB), clampés à maintenant, dédupliqués par (show, jour KST) contre les épisodes réels — home dédup contre les épisodes GLOBAUX (jamais « TBA » quand le lineup est connu) ; global uniquement (un slot n'a pas de groupe), pas de push, pas d'iCal ; 7 tests aux instants UTC vérifiés à la main. (2) Groupes fantômes 0 event/0 MV (VIVIZ — suivi par un vrai user —, ASTRO, MCND, ZICO) : leurs chaînes officielles étaient scrapées mais leurs MVs vivent chez les LABELS (piège playlist uploads §2) ; 4 chaînes labels vérifiées par MVs réels via l'API (BPM Entertainment, fantagio, TOP MEDIA, HYBE LABELS ; 1theK écarté = distributeur multi-artistes) ; backfill +41 MVs (ASTRO 18, MCND 13, VIVIZ 6, ZICO 4), 0 sur-attribution vérifiée. **Décision** : P0.8 tranché OUI (mandat contenu délégué par Rudy 2026-07-11) — le calendrier futur était structurellement mince (23 events, 81 % des groupes sans rien), la grille hebdo fixe des shows est le seul levier read-time.

## 2026-07-11 — Design : exécution du thème (rayons tokens, cartes account dé-v1, error.tsx)

**Branche** : `fix/design-coherence` (2d6a8aa). **Quoi** : rayons px → tokens à équivalence exacte (`rounded-[10px]`→`lg` etc., 39 fichiers, zéro changement visuel — `--radius` propage à nouveau) ; cartes /account dé-v1 (`rounded-2xl`+`shadow-soft` → panneau hairline) ; `error.tsx` aligné sur la 404 (gradient-text, font-heading, buttonVariants) ; `theme-toggle` `text-white`→`text-primary-foreground` (contrat Midnight) ; emerald→`text-teal`, cœurs red-500→token `--live`, opacités disabled unifiées. Issu de l'audit multi-agents 2026-07-10 (dimension design, contre-vérifiée).

## 2026-07-11 — A11y : AA sur le thème par défaut

**Branche** : `fix/a11y-aa` (fba243e). **Quoi** : `--faint` sombre 0.514→0.62 oklch — **5.10:1 sur --card / 5.33:1 sur --background** (avant 3.3:1, sous AA : nav mobile, en-têtes calendrier, stats) ; label de type d'`event-card` sur `EVENT_TYPE_COLORS` calibrés (le hex de marque pastel type ILLIT ≈ 1.4:1 rendait illisible le seul encodage du type) ; ticker pausable (bouton focusable — WCAG 2.2.2, hover inexistant au tactile) ; header-search en vrai combobox ARIA + live region compteur de résultats (4.1.2/4.1.3) ; ring statique de /search → `focus-within` (2.4.7) ; messages push-toggle en `role=alert` (lot UX). Ratios recalculés par script (OKLCH→sRGB→WCAG), pas à l'œil.

## 2026-07-11 — UX : les points d'abandon (dialog mobile, échecs silencieux, vrais 404)

**Branche** : `fix/ux-abandonment-points` (876b653). **Quoi** : `ui/dialog` scrollable (`max-h-[calc(100dvh-2rem)]` + `overflow-y-auto`) + bouton X — l'onglet Artist de « Contribute » (~780 px) rendait Submit inatteignable au mobile sans fermeture visible ; try/catch+toast sur follow/onboarding (échec silencieux, error boundary en plein funnel post-signup) ; confirmation avant « Reset URL » iCal ; copy push « Daily » → « Push notifications » + prefs grisées sans abonnement ; report de commentaire `window.prompt` → Dialog (prompt ignoré par les webviews) ; **soft-404 soldé** — le `loading.tsx` racine forçait le streaming du shell (statut 200 figé avant tout `notFound()`, poison pour le sitemap R2) → skeleton re-scopé par segments listes via `PageSkeleton`, route groups `(home)/` et `groups/(list)/`. **Vérification** : build prod local — `/groups/zzz` → **404**, pages réelles → 200 ; 516 tests.

## 2026-07-11 — Photos membres : nettoyage des fausses + self-host Storage (R4)

**Branche** : `feat/member-photos-selfhost` (9e392c9) + exécution prod. **Quoi** : l'audit a trouvé ~15 photos FAUSSES (graphiques « calendrier du mois » kprofiles servis comme visages de RM/Jin Jin/Renjun/Kwangjin/Chan/Eunwoo, photos du mauvais membre — MJ→Seongwu, Wyatt→MK, Ricky→Hanbin, Jonghun→Seunghyun, clusters TREASURE indécidables) — le self-host prévu les aurait figées. `clean-member-photos.ts` : 14 NULL-ifiées (liste revue à la main ; partages légitimes conservés — Soojin idle↔solo, Youngseo illit↔alldayproject via canonical_id) + re-dérivation kprofiles STRICTE (format galerie seul, le « pairing par ordre » fautif est absent) → 3 récupérées (RM, Renjun, Ricky). `selfhost-member-photos.ts` : bucket public `member-photos`, download UA + repli proxy Cloudinary → **492/492 rapatriées, 0 échec** ; `photo_url` = 100 % Storage (492) + 42 NULL (fallback initiale). Hotlinking kprofiles/up.kpop.re éliminé, re-scrape fraîcheur devenu inutile.

## 2026-07-11 — Data : cause racine des doublons music_show (0040) + dédup same-source

**Branche** : `fix/music-show-stage-url` (a91095a). **Quoi** : colonne dédiée `events.stage_url` — l'enrichissement stage-links écrivait la vidéo du passage dans `source_url`, partie de la clé d'idempotence unique → chaque re-scrape carrd réinsérait une row (14 paires au 07-05, 16 au 07-10, le bug grandissait). Migration 0040 appliquée : 32 rows migrées (stage → stage_url, carrd restauré), 16 doublons purgés (0 réf FK vérifiée), **index unique partiel** `(group_id, start_at) where music_show`. `eventHref` route sur `stage_url` — grouping/iCal/digest en dérivent, un seul point de bascule. Piège d'exécution : la purge doit précéder la restauration (contrainte unique 4-tuple, constaté au 1ᵉʳ apply). + `ingestComebacks` : la dédup ±3 j couvre désormais la MÊME source (kpopofficial poste placeholder puis album finalisé sous 2 URLs — doublon réel fromis_9, purgé, idem tripleS). SCRAPING §3.14 soldé + §3.15 créé. **Vérification** : SQL prod — 0 doublon, 0 source_url YouTube résiduel, 148−16=132 rows.

## 2026-07-11 — CI verte après 18 jours rouges : hourCycle h23

**Branche** : `fix/ci-hourcycle-h23` (18d7379). **Quoi** : `kstTime24h` passait par `hour12: false`, que l'ICU de Node 20 (CI) résout en cycle **h24** — minuit KST rendait « 24:30 » au lieu de « 00:30 » → `date.test.ts:82` rouge sur **chaque run depuis le refactor du 17/06 (~68 runs, dernier vert #180)**, invisible car personne ne regardait la CI après merge. Fix `hourCycle: 'h23'` (déterministe cross-ICU — corrige aussi « 24:00 KST » possible sur de vieux navigateurs pour les drops de minuit) + checkout/setup-node v5 + Node 22 + **règle CLAUDE.md : vérifier le run CI via l'API GitHub après chaque merge**. **Vérification** : run #249 vert, tous les merges suivants de la session verts. Constat : le job e2e est « skipped » depuis toujours (`E2E_ENABLED` jamais posée — action GitHub UI Rudy, la note « CI e2e active » de PROJECT était fausse).

## 2026-07-05 — R3 : export iCal par token (+ 2 bugs réels découverts en route)

**Branche** : `feat/r3-ical-feed` (7 commits). **Quoi** : feed .ics perso par token — migration 0039 `calendar_feeds` (table dédiée : profiles est SELECT-public, un token y fuiterait à tout anon), générateur RFC 5545 pur (folding byte-aware UTF-8 75 octets, CRLF, UID stables, DTSTAMP déterministe), mapping domaine (épisodes music show fusionnés = 1 VEVENT « Music Bank — H2H, izna +1 », anniversaires all-day sur le jour KST avec année dans l'UID), route `GET /api/ical/[token]` (404 uniforme, cache CDN 1 h), section /account (Enable lazy, copy URL, Reset). V1 free intégral. **Vérification** : 515 tests (23 nouveaux) ; flux réel — Enable via l'UI → token en DB → curl du .ics → 21 VEVENTs corrects (épisode futur groupé, stages passés individuels, release, anniversaires all-day). **Bugs réels trouvés par le test du feed** : (1) **doublons DB music_show** — l'enrichissement stage-links change `source_url` qui fait partie de la clé d'idempotence du scraper → le scrape suivant réinsère la row carrd (14 paires en prod) ; dédup display-level livrée dans `groupMusicShowEpisodes` (la row enrichie gagne — corrige aussi l'app), cause racine au BACKLOG ; (2) **le golden path E2E détruisait des données** — `getByRole({ name: 'Follow' })` matche « Unfollow » par sous-chaîne → chaque run désabonnait un groupe du compte test (le compte réel de Rudy !) ; fix `exact: true`, follows restaurés (6 — à vérifier par Rudy). **Décisions** : pas de rate-limit V1 (token 122 bits + CDN) ; feeds filtrés multi-calendriers = premium futur.

## 2026-07-05 — Fix CI : job e2e stable (build prod + specs durcies)

**Branche** : `fix/ci-e2e` (e3027b8). **Quoi** : tous les runs CI du jour étaient rouges depuis l'activation du job e2e le matin. Trois causes réelles trouvées en reproduisant le mode CI en local : (1) dev server Turbopack froid compilant chaque route pendant 28 tests parallèles → timeouts ; fix = step « Build (production) » dédié + `next start` comme webServer CI ; (2) **violations strict-mode latentes** — badges landing en double dans le DOM, et le header search rend son propre panneau (« Top result », « No results ») dès que `?q=` est dans l'URL → doublons avec la page /search ; en dev l'hydratation lente masquait la course, un serveur rapide la révèle ; fix = `.first()` + assertions scopées `main` ; (3) golden path auth en double (chromium+mobile) sur le **même compte** partagé (mutations follows + signOut Supabase global) → interférences ; fix = chromium seul. + `retries: 1` et `workers: 2` en CI. **Vérification** : 4 runs CI-mode locaux consécutifs verts ; mode dev inchangé. **Leçon** : un E2E vert en dev ne prouve pas le mode prod — les courses d'hydratation changent de camp.

## 2026-07-05 — SEO programmatique (R2) : sitemap 1403 URLs, JSON-LD, OG dynamiques

**Branche** : `feat/seo-programmatic` (4 commits). **Quoi** : (1) **sitemap dynamique — 5 routes statiques → 1403 URLs** (groupes non-solo, 826 MVs avec lastModified réel, ~490 artistes canoniques ; exclusions = redirects ; revalidate 86400 ; client anon nu, pas de cookies dans ce contexte) ; (2) JSON-LD : MusicGroup sur les fiches groupe (sameAs = links sociaux, foundingDate), VideoObject sur les MVs (+aggregateRating /10 si noté) via helper `JsonLd` (échappement `<`) ; (3) metadata manquantes : /calendar, /search, /mvs (canonical), /artists/[slug] (aucune avant), og sur /mv ; (4) OG images dynamiques par groupe (« Next: {drop} — {date} KST ») et par MV (titre + groupe + date), version marque. **Pourquoi** : audit acquisition 2026-07-05 — ~940 pages invisibles des moteurs, zéro donnée structurée, une seule OG statique ; le SEO programmatique est le canal d'acquisition passive n°1 sans budget. **Vérification** : 1403 `<loc>` servies ; JSON-LD rendu avec données réelles (aespa : sameAs 8 links) ; OG PNG vérifiées visuellement (aespa affiche son vrai prochain drop « KISS N TELL — Jul 24 KST ») ; 501 tests + E2E verts. **Décisions** : Event schema rejeté (pas de page dédiée, venue/offers exigés) ; OG photo+Archivo = itération backlog (fonts custom par génération + images distantes = L) ; **vigilance** : les ~490 pages membres sont thin — si Search Console le signale, retirer les artistes du sitemap (décision page-pruning de juin).

## 2026-07-05 — Boucle push complète (R1) : opt-in onboarding + préférences par type

**Branche** : `feat/push-loop` (4 commits). **Quoi** : (1) le digest push deep-link vers `/calendar` (avant : `/`) ; (2) **étape opt-in notifs dans l'onboarding** juste après le follow — le funnel ne proposait jamais le push, moteur de rétention n°1 ; in-component (une route séparée retombait sur la garde `followed.size>0`), toutes les branches sortent du funnel (denied/unsupported → Continue, iOS Safari → hint d'install) ; (3) **préférences par type** sur /account (5 toggles, état optimiste) branchées aux deux crons via `Map<userId, Set<type>>` (1 query `.eq(enabled,false)` par cron, pas de N+1 ; param optionnel des builders purs = zéro régression). **Pourquoi** : audit rétention 2026-07-05 — `user_notification_settings` était une table fantôme (jamais lue, aucune UI), les crons arrosaient tous les abonnés. **Vérification** : 501 tests (6 nouveaux) ; toggle réel via login E2E → row en DB → persistant au reload → restauré ; **preuve de bout en bout** : digest hebdo déclenché avec music_show OFF → push reçu « Your k-pop week: 1 event » (la release i-dle seule, le Music Bank à 4 groupes suivis exclu). **Décisions** : zéro migration (policy RLS FOR ALL own-rows en place depuis 0002) ; absence de row = activé (pas de seed) ; frontière free/premium posée — on/off par type = free, `lead_time_minutes`/multi-rappels = premium futur (default 1440 intact, rien construit) ; concert/other non exposés. À noter : les creds E2E de `.env.local` pointent sur le compte réel de Rudy, pas kstage-e2e.

## 2026-07-05 — Music shows : regroupement par épisode à l'affichage

**Branche** : `feat/music-show-grouping`. **Quoi** : un épisode de music show stocké N lignes (une par groupe du lineup) s'affiche désormais en **1 carte** avec le lineup en sous-titre (« ATEEZ, Hearts2Hearts, izna +2 ») sur home (queue + hero + preview landing), week glance, calendrier, search — et le digest push agrège pareil (« Music Bank (5 artists) »). **Pourquoi** : le Music Bank du 10/07 apparaissait 5 fois à l'identique — redondance visible dès la home. **Comment** : fonction pure `groupMusicShowEpisodes` dans `grouping.ts` (clé `title|start_at`, title = enum fixe de 6 shows) ; **prédicat d'exclusion : seules les lignes au href interne fusionnent** — après enrichissement stage-links chaque ligne porte SON stage YouTube par groupe et reste individuelle ; groupement APRÈS le filtre follows (lineup = tes groupes) et AVANT les caps (un épisode ne mange plus 5 slots de queue) ; carte groupée → deep-link jour du calendrier (seul « lieu » commun au lineup) ; compteurs (chips, cellules, week glance) suivent par construction ; ticker non touché (déduplique déjà). **Vérification** : 495 tests unitaires (12 nouveaux sur fixtures prod) ; 28/28 E2E ; visuel réel — calendrier 10/07 : 1 carte + lineup + href jour ✓ ; search : épisode passé « aespa, Fifty Fifty, MEOVV +3 » groupé ✓. **Décisions** : lineup en texte (pas de liens imbriqués dans le root Link) ; pas de migration DB ni de changement scraper (affaire d'affichage) ; l'idée « show comme entrée suivable » reste NON faite (backlog).

## 2026-07-05 — Quick wins UX : partage MV + hint install iOS

**Branche** : `feat/ux-quick-wins`. **Quoi** : `ShareButton` ajouté sur `/mv/[slug]` (top-right du player, symétrie du BackButton — pattern ArtistHero) ; `IosInstallHint` monté en fin de colonne centrale de la home connectée (auto-gated Safari iOS hors standalone). **Pourquoi** : deux composants finis qui dormaient — le partage n'existait que sur les fiches artiste, le hint que sur /account. **Vérification** : Playwright réel — clic Share → toast « Link copied » + URL dans le presse-papier ; login E2E en UA iPhone → hint visible sur la home connectée, absent en desktop/landing ; 28/28 E2E. **Décisions** : pas de ShareButton sur les cartes event (root `<Link>`, bouton imbriqué invalide) ; pas de hint sur la landing (funnel signup non dilué). Vérifié au passage : le toast succès des suggestions existait déjà (item backlog clos sans code). **Incident réparé en route** : `NEXT_PUBLIC_SUPABASE_URL` vidée dans `.env.local` (édition du matin, valeur coupée au lieu de copiée) → toutes les pages en 500 en local ; valeur restaurée.

## 2026-07-05 — Journal de bord versionné

**Branche** : `docs/journal`. **Quoi** : création de ce fichier (seed rétroactif depuis PROJECT.md §9), PROJECT.md §9 raccourci à l'état courant, règle d'entretien ajoutée à CLAUDE.md. **Pourquoi** : demande Rudy — tenir les docs comme un journal de bord ; l'historique daté s'accumulait dans PROJECT.md §9 qui devenait illisible. **Décisions** : une entrée par merge ; l'état courant reste dans PROJECT.md ; les décisions produit cross-session vont aussi dans la memory `project_decisions_ledger`.

## 2026-07-04 — Plan de reprise : socle sécurité + rétention (5 lots)

**Branches** : `chore/deps-undici`, `fix/rate-limit-atomic`, `chore/e2e-search-feedback`, `chore/csp-enforce`, `feat/weekly-digest` (commits 9d79916 → 827a0ca). **Quoi** : (1) undici 7.28.0 — les 7 advisories GitHub tombent à 0 ; (2) rate-limits **atomiques** via RPC `consume_rate_limit` (migration 0038, advisory lock) sur 5 writes dont un cap nouveau 20/24h sur push-subscribe ; (3) E2E `/search` (6 tests) + widget feedback (3 tests, UI sans submit) ; (4) **CSP en enforce** en prod (report-only en dev), `unsafe-eval` retiré en prod ; (5) digest **hebdo** « Your k-pop week » le lundi (fenêtre 7 j, même cron — Hobby 1×/jour). **Vérification** : probe SQL atomicité `tttff` (cap 3) ; 0 violation CSP sur 7 pages prod ; 28/28 E2E contre le build enforce ; push hebdo réel reçu (`{edition:weekly, sent:2}`). **Décisions** : nonces CSP rejetés (forcent le rendu dynamique global, tue static/ISR ; `style-src` nonce impasse avec les attributs `style=""` React) → gated pré-ouverture publique ; bug latent attrapé par la vérif enforce : `connect-src` ne couvrait pas `wss://*.supabase.co` (Realtime commentaires). Docs : `RISKS.md` créé (jina SPOF, plafonds free tiers ~45 $/mois, minimum maintenable). Baseline advisors élargi : +2 warnings intentionnels (deny-all rate_limit_hits, SECURITY DEFINER consume_rate_limit).

## 2026-07-02→04 — Refonte « Data Desk » + bascule qualité data

**Branches** : 4 merges sur main (dont `feat/design-data-desk`, `feat/post-redesign-hardening`, `feat/landing-search-stages`, `feat/audit-hardening`). **Quoi** : refonte visuelle complète — tokens oklch (thèmes Daylight/Midnight), typo Archivo condensée (labels), nav 5 items, home 8 modules (ticker, hero sur le dernier MV, queue, week glance, fresh drops), calendrier, Drops, fiches groupe/MV, `/search` (recherche live multi-termes), profil, landing triée par notoriété YouTube. **Data** : purge de **202 faux MVs** (teasers, behind, sketchs — `SCRAPING.md §3.13`) → 815 MVs propres ; gates scraper durcies (blacklist étendue + durée ≥ 75 s). **Stage links** : events music_show enrichis quotidiennement vers la vidéo YouTube du stage (`SCRAPING.md §9`). Widget **Feedback** (table `feedback`, 2/24h, `/admin/feedback`) — canal de signalement des fausses données. Audit global final : 4 bugs, 2 DRY, 7 UX (commit 3013df9). **Pourquoi** : « si de faux MVs réapparaissent je devrai fermer l'app » (Rudy) — la qualité data est existentielle ; le design pastel ne différenciait pas. **Décisions** : popularité = `sources.subscriber_count` (Spotify inutilisable en dev-mode) ; couleurs Data Desk mv=release=teal ; wording « Drops/MV ».

## 2026-06-13→16 — P0 data (couverture) + P1 hardening + thème

**Quoi** : scraper YouTube réécrit sur `playlistItems.list` (27 units/run vs 1 600) ; couverture élargie 4 → 44 groupes (discovery oembed multi-agents, backfill 108 → 402 MVs, 0 sur-attribution) ; matching kpopofficial élargi (+9 events dont 5 futurs) ; SPOF kpopofficial cassé (Wikipedia « 2026 in South Korean music » = 2ᵉ source, cœur partagé `comeback-ingest.ts`, dédup cross-source ±3 j) ; music shows : couverture = semaine courante documentée (pas un bug parser — les sources ne publient que tardivement). P1 : SEO pages groupes, advisors sécurité (0033-0035 : revoke handle_new_user, leaked-password activée, buckets listing), perf RLS `(select auth.uid())` sur 21 policies, helper CRON_SECRET constant-time, Vitest env node (suite 29 s → 6 s). Thème Daylight/Midnight + refonte handoff design (couleurs→tokens, ombres, labels mono). **Vérification** : advisors re-runés à 0 (hors baseline acceptée) ; fixture kpopofficial réelle → bug de couverture découvert et corrigé (migration `.gspb-dynamic-title-element`). **Décisions** : critère top-groupes = subs YouTube (Spotify followers inaccessibles, vérifié en direct).

## 2026-06-12 — Audit projet + direction reset

**Quoi** : audit complet vérifié en prod (`AUDIT_PROJET_2026-06-12.md`) : 320 events dont **8 futurs seulement**, 82 % des groupes sans event, 100 % du futur dépendant de kpopofficial — la donnée, pas le code, est le chantier. BACKLOG réécrit data-first (P0 data → P1 quick wins → P2 habitude). **Décisions (Rudy)** : pas de date de soft launch ; objectif = **V1 assez bonne pour retenir dès le premier utilisateur** ; toute feature dépendante du nombre d'users gelée jusqu'à audience réelle (règle de gel).

## 2026-05-29→30 — MVP terminé + V2 beachhead

**Quoi** : MVP 9 étapes clôturé (auth, follows, scraping YouTube/kpopofficial/6 music shows, push, suggestions communautaires + modération admin, SEO/landing/a11y Lighthouse 100). Post-MVP : home 3 colonnes ; Phase 4 community (migration 0009 : `event_ratings`, `comments` + votes Reddit-style, tri top/new, soft-delete sur `/mv/[slug]`) ; member pages canonical (0011-0013 : `members.slug/photo/status`, `canonical_id` self-FK — 1 page par personne, tabs Groups/Solo) ; trigger `handle_new_user()` (0015) ; redirect 308 solos. Seed photos kpopnet CC0 (349/833 patchés). **Décisions** : V2 beachhead = ratings+commentaires ancrés aux events scrapés (pattern hltv : la data attire, la commu retient — pas de forum avant audience).

## 2026-05-23→28 — MVP étapes 1-7 (setup → scraping)

**Quoi** : setup Next.js 16 + Supabase + Vercel ; modèle 8 tables + RLS ; frontend basique ; auth + follows ; pipeline YouTube Data API (premieres) ; push Web Push API (SW à la main) ; comebacks kpopofficial. **Leçons (étape 6)** : Vercel Cron déclenche en **GET uniquement** (bug latent corrigé : scrape-youtube était POST-only, jamais déclenché) ; Next 16 = Turbopack → `@serwist/next` (webpack) inapplicable, SW manuel suffit pour du push pur ; `pushManager.subscribe` peut throw (navigation privée, push service indispo) → try/catch obligatoire côté UI. **Décisions** : dbkpop abandonné (mort), kpopping écarté (anti-bot) ; Vercel Cron primaire (limite Hobby 1×/jour cohérente avec le besoin).
